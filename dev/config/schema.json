{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "title": "AppConfig",
  "type": "object",
  "properties": {
    "log_level": {
      "type": "string",
      "default": "info"
    },
    "logger": {
      "type": "string",
      "default": ""
    },
    "ui_addr": {
      "description": "Optional url of the ui endpoint. For example \"0.0.0.0:9090\".",
      "type": "string",
      "default": ""
    },
    "metrics_addr": {
      "description": "Optional url of a standalone metrics endpoint. For example \"0.0.0.0:9091\".\nIf set, a standalone metrics server will be started on this address.\nIf it matches `ui_addr`, the standalone server is skipped as the UI handles it.",
      "type": "string",
      "default": ""
    },
    "plugins": {
      "description": "Native plugin libraries to load before starting anything. Each provides\nan endpoint (and possibly a middleware) usable by name in routes.",
      "type": "array",
      "items": {
        "type": "string"
      }
    },
    "routes": {
      "type": "object",
      "additionalProperties": {
        "$ref": "#/$defs/RouteConfig"
      }
    },
    "consumers": {
      "type": "array",
      "items": {
        "$ref": "#/$defs/ConsumerConfig"
      },
      "default": []
    },
    "publishers": {
      "type": "array",
      "items": {
        "$ref": "#/$defs/PublisherClient"
      },
      "default": []
    },
    "history": {
      "type": "object",
      "additionalProperties": true
    },
    "env_vars": {
      "type": "object",
      "additionalProperties": {
        "type": "string"
      },
      "default": {}
    },
    "config_security": {
      "anyOf": [
        {
          "$ref": "#/$defs/ConfigSecurity"
        },
        {
          "type": "null"
        }
      ]
    },
    "extract_secrets": {
      "description": "Legacy compatibility flag. Prefer config_security.mode instead.",
      "type": "boolean"
    },
    "default_tab": {
      "description": "The default tab to show in the UI upon loading.",
      "type": "string",
      "default": ""
    }
  },
  "$defs": {
    "RouteConfig": {
      "description": "Defines a single message processing route from an input to an output.",
      "type": "object",
      "properties": {
        "enabled": {
          "type": "boolean",
          "default": true
        },
        "input": {
          "description": "The input/source endpoint for the route.",
          "$ref": "#/$defs/Endpoint"
        },
        "output": {
          "description": "The output/sink endpoint for the route.",
          "anyOf": [
            {
              "$ref": "#/$defs/Endpoint"
            },
            {
              "type": "null"
            }
          ],
          "default": {
            "middlewares": [],
            "null": null
          }
        },
        "description": {
          "description": "A human-readable description of the route's purpose. Defaults to an empty string.",
          "type": "string"
        },
        "concurrency": {
          "description": "(Optional) Number of concurrent processing tasks for this route. While it improves throughput for high-latency\nhandlers, it adds synchronization overhead for ordered commits and may lead to out-of-order processing\nin the handler. Above 1, whole batches may also reach the sink out of source order (rows keep their order\nwithin a batch) unless the sink declares itself order-sensitive, as `file` does.\nDefaults to 1.",
          "type": "integer",
          "format": "uint",
          "minimum": 1,
          "default": 1
        },
        "batch_size": {
          "description": "(Optional) Maximum number of messages to process in a single batch. The consumer waits for at least one message\nand then attempts to fetch more if available. Increasing this improves throughput but also increases\nthe potential impact of a single batch processing failure. Defaults to 512.",
          "type": "integer",
          "format": "uint",
          "minimum": 1,
          "default": 512
        },
        "commit_concurrency_limit": {
          "description": "(Optional) The maximum number of in-flight commit requests queued for ordered sequencing.\nLower values apply backpressure earlier; higher values allow larger commit backlogs.\nDefaults to 4096.",
          "type": "integer",
          "format": "uint",
          "minimum": 1,
          "default": 4096
        },
        "startup_timeout_ms": {
          "description": "Time to wait for a route to establish connections before startup fails. Defaults to 5000ms.",
          "type": "integer",
          "format": "uint64",
          "minimum": 0,
          "default": 5000
        },
        "reconnect_interval_ms": {
          "description": "Time to wait before reconnecting after a transient route failure. Defaults to 5000ms.",
          "type": "integer",
          "format": "uint64",
          "minimum": 0,
          "default": 5000
        },
        "empty_batch_delay_ms": {
          "description": "Delay after an empty receive batch to avoid hot polling. Set to 0 to only yield. Defaults to 10ms.",
          "type": "integer",
          "format": "uint64",
          "minimum": 0,
          "default": 10
        },
        "allow_fault_injection": {
          "description": "Allows fault-injection middleware such as random_panic. Disabled by default.",
          "type": "boolean",
          "default": false
        },
        "exit_on_empty": {
          "description": "If true, the route exits gracefully once the source yields an empty batch\n(drain-then-exit). Off by default — routes normally poll indefinitely.\nA drain that keeps failing to reconnect gives up and fails rather than retrying forever.",
          "type": "boolean"
        }
      },
      "required": [
        "input"
      ]
    },
    "Endpoint": {
      "description": "Represents a connection point for messages, which can be a source (input) or a sink (output).",
      "type": "object",
      "properties": {
        "middlewares": {
          "description": "(Optional) A list of middlewares to apply to the endpoint.",
          "type": "array",
          "items": {
            "$ref": "#/$defs/Middleware"
          },
          "default": []
        }
      },
      "unevaluatedProperties": false,
      "oneOf": [
        {
          "type": "object",
          "properties": {
            "aws": {
              "$ref": "#/$defs/AwsConfig"
            }
          },
          "required": [
            "aws"
          ]
        },
        {
          "type": "object",
          "properties": {
            "kafka": {
              "$ref": "#/$defs/KafkaConfig"
            }
          },
          "required": [
            "kafka"
          ]
        },
        {
          "type": "object",
          "properties": {
            "nats": {
              "$ref": "#/$defs/NatsConfig"
            }
          },
          "required": [
            "nats"
          ]
        },
        {
          "type": "object",
          "properties": {
            "file": {
              "$ref": "#/$defs/FileConfig"
            }
          },
          "required": [
            "file"
          ]
        },
        {
          "type": "object",
          "properties": {
            "object_store": {
              "$ref": "#/$defs/ObjectStoreConfig"
            }
          },
          "required": [
            "object_store"
          ]
        },
        {
          "type": "object",
          "format": "structural_endpoint",
          "properties": {
            "static": {
              "$ref": "#/$defs/StaticConfig"
            }
          },
          "required": [
            "static"
          ]
        },
        {
          "type": "object",
          "format": "structural_endpoint",
          "properties": {
            "ref": {
              "type": "string"
            }
          },
          "required": [
            "ref"
          ]
        },
        {
          "type": "object",
          "properties": {
            "memory": {
              "$ref": "#/$defs/MemoryConfig"
            }
          },
          "required": [
            "memory"
          ]
        },
        {
          "type": "object",
          "properties": {
            "sled": {
              "$ref": "#/$defs/SledConfig"
            }
          },
          "required": [
            "sled"
          ]
        },
        {
          "type": "object",
          "properties": {
            "amqp": {
              "$ref": "#/$defs/AmqpConfig"
            }
          },
          "required": [
            "amqp"
          ]
        },
        {
          "type": "object",
          "properties": {
            "mongodb": {
              "$ref": "#/$defs/MongoDbConfig"
            }
          },
          "required": [
            "mongodb"
          ]
        },
        {
          "type": "object",
          "properties": {
            "mqtt": {
              "$ref": "#/$defs/MqttConfig"
            }
          },
          "required": [
            "mqtt"
          ]
        },
        {
          "type": "object",
          "properties": {
            "http": {
              "$ref": "#/$defs/HttpConfig"
            }
          },
          "required": [
            "http"
          ]
        },
        {
          "type": "object",
          "properties": {
            "websocket": {
              "$ref": "#/$defs/WebSocketConfig"
            }
          },
          "required": [
            "websocket"
          ]
        },
        {
          "type": "object",
          "properties": {
            "ibmmq": {
              "$ref": "#/$defs/IbmMqConfig"
            }
          },
          "required": [
            "ibmmq"
          ]
        },
        {
          "type": "object",
          "properties": {
            "zeromq": {
              "$ref": "#/$defs/ZeroMqConfig"
            }
          },
          "required": [
            "zeromq"
          ]
        },
        {
          "type": "object",
          "properties": {
            "redis_streams": {
              "$ref": "#/$defs/RedisStreamsConfig"
            }
          },
          "required": [
            "redis_streams"
          ]
        },
        {
          "type": "object",
          "properties": {
            "grpc": {
              "$ref": "#/$defs/GrpcConfig"
            }
          },
          "required": [
            "grpc"
          ]
        },
        {
          "type": "object",
          "properties": {
            "sqlx": {
              "$ref": "#/$defs/SqlxConfig"
            }
          },
          "required": [
            "sqlx"
          ]
        },
        {
          "type": "object",
          "properties": {
            "clickhouse": {
              "$ref": "#/$defs/ClickHouseConfig"
            }
          },
          "required": [
            "clickhouse"
          ]
        },
        {
          "type": "object",
          "properties": {
            "postgres_cdc": {
              "$ref": "#/$defs/PostgresCdcConfig"
            }
          },
          "required": [
            "postgres_cdc"
          ]
        },
        {
          "type": "object",
          "format": "structural_endpoint",
          "properties": {
            "fanout": {
              "type": "array",
              "items": {
                "$ref": "#/$defs/Endpoint"
              }
            }
          },
          "required": [
            "fanout"
          ]
        },
        {
          "type": "object",
          "format": "structural_endpoint",
          "properties": {
            "stream_buffer": {
              "$ref": "#/$defs/StreamBufferConfig"
            }
          },
          "required": [
            "stream_buffer"
          ]
        },
        {
          "type": "object",
          "format": "structural_endpoint",
          "properties": {
            "switch": {
              "$ref": "#/$defs/SwitchConfig"
            }
          },
          "required": [
            "switch"
          ]
        },
        {
          "type": "object",
          "format": "structural_endpoint",
          "properties": {
            "response": {
              "$ref": "#/$defs/ResponseConfig"
            }
          },
          "required": [
            "response"
          ]
        },
        {
          "type": "object",
          "format": "structural_endpoint",
          "properties": {
            "reader": {
              "$ref": "#/$defs/Endpoint"
            }
          },
          "required": [
            "reader"
          ]
        },
        {
          "type": "object",
          "format": "structural_endpoint",
          "properties": {
            "request": {
              "$ref": "#/$defs/RequestForwardConfig"
            }
          },
          "required": [
            "request"
          ]
        },
        {
          "type": "object",
          "format": "structural_endpoint",
          "properties": {
            "custom": {
              "type": "object",
              "properties": {
                "name": {
                  "type": "string"
                },
                "config": true
              },
              "required": [
                "name",
                "config"
              ]
            }
          },
          "required": [
            "custom"
          ]
        },
        {
          "type": "object",
          "format": "structural_endpoint",
          "properties": {
            "null": {
              "type": "null"
            }
          },
          "required": [
            "null"
          ]
        }
      ]
    },
    "Middleware": {
      "description": "An enumeration of all supported middleware types.",
      "oneOf": [
        {
          "description": "Renders a template into the `mqb.id` metadata key, e.g. `id: \"${payload:order_id}\"`.\nConsumer-only; list it *after* anything that reads `mqb.id`, since the last consumer\nmiddleware in the list runs first.",
          "type": "object",
          "properties": {
            "id": {
              "type": "string"
            }
          },
          "required": [
            "id"
          ],
          "additionalProperties": false
        },
        {
          "type": "object",
          "properties": {
            "deduplication": {
              "$ref": "#/$defs/DeduplicationMiddleware"
            }
          },
          "required": [
            "deduplication"
          ],
          "additionalProperties": false
        },
        {
          "type": "object",
          "properties": {
            "metrics": {
              "$ref": "#/$defs/MetricsMiddleware"
            }
          },
          "required": [
            "metrics"
          ],
          "additionalProperties": false
        },
        {
          "type": "object",
          "properties": {
            "dlq": {
              "$ref": "#/$defs/DeadLetterQueueMiddleware"
            }
          },
          "required": [
            "dlq"
          ],
          "additionalProperties": false
        },
        {
          "type": "object",
          "properties": {
            "retry": {
              "$ref": "#/$defs/RetryMiddleware"
            }
          },
          "required": [
            "retry"
          ],
          "additionalProperties": false
        },
        {
          "type": "object",
          "properties": {
            "random_panic": {
              "$ref": "#/$defs/RandomPanicMiddleware"
            }
          },
          "required": [
            "random_panic"
          ],
          "additionalProperties": false
        },
        {
          "type": "object",
          "properties": {
            "delay": {
              "$ref": "#/$defs/DelayMiddleware"
            }
          },
          "required": [
            "delay"
          ],
          "additionalProperties": false
        },
        {
          "type": "object",
          "properties": {
            "weak_join": {
              "$ref": "#/$defs/WeakJoinMiddleware"
            }
          },
          "required": [
            "weak_join"
          ],
          "additionalProperties": false
        },
        {
          "type": "object",
          "properties": {
            "limiter": {
              "$ref": "#/$defs/LimiterMiddleware"
            }
          },
          "required": [
            "limiter"
          ],
          "additionalProperties": false
        },
        {
          "type": "object",
          "properties": {
            "buffer": {
              "$ref": "#/$defs/BufferMiddleware"
            }
          },
          "required": [
            "buffer"
          ],
          "additionalProperties": false
        },
        {
          "type": "object",
          "properties": {
            "cookie_jar": {
              "$ref": "#/$defs/CookieJarMiddleware"
            }
          },
          "required": [
            "cookie_jar"
          ],
          "additionalProperties": false
        },
        {
          "type": "object",
          "properties": {
            "transform": {
              "$ref": "#/$defs/TransformMiddleware"
            }
          },
          "required": [
            "transform"
          ],
          "additionalProperties": false
        },
        {
          "type": "object",
          "properties": {
            "encryption": {
              "$ref": "#/$defs/EncryptionConfig"
            }
          },
          "required": [
            "encryption"
          ],
          "additionalProperties": false
        },
        {
          "type": "object",
          "properties": {
            "compression": {
              "$ref": "#/$defs/CompressionMiddleware"
            }
          },
          "required": [
            "compression"
          ],
          "additionalProperties": false
        },
        {
          "type": "object",
          "properties": {
            "custom": {
              "type": "object",
              "properties": {
                "name": {
                  "type": "string"
                },
                "config": true
              },
              "required": [
                "name",
                "config"
              ]
            }
          },
          "required": [
            "custom"
          ],
          "additionalProperties": false
        }
      ]
    },
    "DeduplicationMiddleware": {
      "description": "Deduplication middleware configuration.\n\nPrevents duplicate messages from being processed using a sled, MongoDB, or SQL backend.\nMessages are identified by their deduplication key and removed after the TTL expires.",
      "type": "object",
      "properties": {
        "store": {
          "description": "Store URL: `sled:///path` (local), `mongodb://host/db[/collection]`, or `postgres|mysql|mariadb|sqlite://…[/table]` (shared).",
          "type": [
            "string",
            "null"
          ],
          "default": null
        },
        "sled_path": {
          "description": "Local Sled directory (legacy). Prefer `store`.",
          "type": [
            "string",
            "null"
          ],
          "default": null
        },
        "ttl_seconds": {
          "description": "Time-to-live for deduplication entries in seconds.",
          "type": "integer",
          "format": "uint64",
          "minimum": 0
        },
        "key": {
          "description": "Dedup key template, e.g. `${payload:order_id}`. Defaults to `message_id`.",
          "type": [
            "string",
            "null"
          ],
          "default": null
        }
      },
      "additionalProperties": false,
      "required": [
        "ttl_seconds"
      ]
    },
    "MetricsMiddleware": {
      "description": "Metrics middleware configuration.\n\nEnables collection and reporting of message processing metrics such as throughput,\nlatency, and error rates. The presence of this middleware in the configuration\nenables metrics collection for the endpoint.\n\nMetrics are typically exported via Prometheus or similar monitoring systems.",
      "type": "object",
      "additionalProperties": false
    },
    "DeadLetterQueueMiddleware": {
      "description": "Dead-Letter Queue (DLQ) middleware configuration.\n\nRoutes failed messages to a designated endpoint for later analysis and recovery.\nIt is recommended to pair this with the Retry middleware to avoid message loss.\n\nFailed messages are sent to the configured endpoint when they are exhausted after retry attempts.",
      "type": "object",
      "properties": {
        "endpoint": {
          "description": "The endpoint to send failed messages to.",
          "$ref": "#/$defs/Endpoint"
        }
      },
      "additionalProperties": false,
      "required": [
        "endpoint"
      ]
    },
    "RetryMiddleware": {
      "description": "Retry middleware configuration.\n\nImplements exponential backoff retry logic for failed message processing.\nFailed messages are automatically retried with increasing delays between attempts.",
      "type": "object",
      "properties": {
        "max_attempts": {
          "description": "Maximum number of retry attempts. Defaults to 3.",
          "type": "integer",
          "format": "uint",
          "minimum": 0,
          "default": 3
        },
        "initial_interval_ms": {
          "description": "Initial retry interval in milliseconds. Defaults to 100ms.",
          "type": "integer",
          "format": "uint64",
          "minimum": 0,
          "default": 100
        },
        "max_interval_ms": {
          "description": "Maximum retry interval in milliseconds. Defaults to 5000ms.",
          "type": "integer",
          "format": "uint64",
          "minimum": 0,
          "default": 5000
        },
        "multiplier": {
          "description": "Multiplier for exponential backoff. Defaults to 2.0.",
          "type": "number",
          "format": "double",
          "default": 2.0
        }
      },
      "additionalProperties": false
    },
    "RandomPanicMiddleware": {
      "description": "Middleware for fault injection testing.\n\nAllows testing error handling and recovery mechanisms by injecting faults\nat specific points in the message processing pipeline.\n\n# Examples\n\n```yaml\nrandom_panic:\n  mode: panic\n  trigger_on_message: 3  # Trigger on the 3rd message\n```",
      "type": "object",
      "properties": {
        "mode": {
          "description": "The type of fault to inject.",
          "$ref": "#/$defs/FaultMode",
          "default": "panic"
        },
        "trigger_on_message": {
          "description": "Trigger the fault on the Nth message (1-indexed). None = trigger on every message.",
          "type": [
            "integer",
            "null"
          ],
          "format": "uint",
          "minimum": 1,
          "default": null
        },
        "enabled": {
          "description": "Enable/disable the fault injection without removing the configuration.",
          "type": "boolean",
          "default": true
        }
      },
      "additionalProperties": false
    },
    "FaultMode": {
      "description": "Fault injection modes for testing error handling and recovery mechanisms.",
      "oneOf": [
        {
          "description": "Trigger a thread panic.",
          "type": "string",
          "const": "panic"
        },
        {
          "description": "Simulate a connection/network error (retryable).",
          "type": "string",
          "const": "disconnect"
        },
        {
          "description": "Simulate a timeout error (retryable).",
          "type": "string",
          "const": "timeout"
        },
        {
          "description": "Simulate a JSON format error (non-retryable).",
          "type": "string",
          "const": "json_format_error"
        },
        {
          "description": "Return a negative acknowledgement (for handlers).",
          "type": "string",
          "const": "nack"
        }
      ]
    },
    "DelayMiddleware": {
      "description": "Delay middleware configuration.\n\nIntroduces a fixed delay before processing each message.\nUseful for rate limiting, testing, or allowing time for dependent systems to become ready.",
      "type": "object",
      "properties": {
        "delay_ms": {
          "description": "Delay duration in milliseconds.",
          "type": "integer",
          "format": "uint64",
          "minimum": 0
        }
      },
      "additionalProperties": false,
      "required": [
        "delay_ms"
      ]
    },
    "WeakJoinMiddleware": {
      "description": "Weak Join middleware configuration.\n\nCorrelates messages by a metadata key and joins them within a timeout window.\nCount mode (default) waits for `expected_count` messages and emits a JSON array.\nBranch mode (set `branch_by`) waits for named branches and emits a branch-keyed object.",
      "type": "object",
      "properties": {
        "group_by": {
          "description": "The metadata key to group messages by (e.g., \"correlation_id\").",
          "type": "string"
        },
        "expected_count": {
          "description": "The number of messages (count mode) or distinct branches (branch mode) to wait for.",
          "type": "integer",
          "format": "uint",
          "minimum": 0
        },
        "timeout_ms": {
          "description": "Timeout in milliseconds.",
          "type": "integer",
          "format": "uint64",
          "minimum": 0
        },
        "branch_by": {
          "description": "Metadata key naming each message's branch; enables branch mode when set.",
          "type": [
            "string",
            "null"
          ],
          "default": null
        },
        "required": {
          "description": "Branch names that must all arrive before firing (branch mode; overrides expected_count).",
          "type": "array",
          "items": {
            "type": "string"
          },
          "default": []
        },
        "on_timeout": {
          "description": "What to do with an incomplete group when the timeout expires.",
          "$ref": "#/$defs/WeakJoinTimeout",
          "default": "fire"
        }
      },
      "additionalProperties": false,
      "required": [
        "group_by",
        "expected_count",
        "timeout_ms"
      ]
    },
    "WeakJoinTimeout": {
      "description": "Action taken on an incomplete weak-join group when its timeout expires.",
      "oneOf": [
        {
          "description": "Emit the partial join (current behavior).",
          "type": "string",
          "const": "fire"
        },
        {
          "description": "Drop the incomplete group without emitting.",
          "type": "string",
          "const": "discard"
        }
      ]
    },
    "LimiterMiddleware": {
      "description": "Throughput limiter middleware configuration.\n\nApplies a best-effort pacing delay so an endpoint does not exceed the configured\nmessage rate. For batch operations the limiter accounts for the number of messages\nin the batch, not just the batch count.",
      "type": "object",
      "properties": {
        "messages_per_second": {
          "description": "Target throughput in messages per second. Must be greater than zero.",
          "type": "number",
          "format": "double"
        }
      },
      "additionalProperties": false,
      "required": [
        "messages_per_second"
      ]
    },
    "BufferMiddleware": {
      "description": "Publisher-side buffer middleware configuration.\n\nBuffers outbound messages briefly so multiple single-message sends can be\nforwarded as one `send_batch` call to the wrapped publisher.",
      "type": "object",
      "properties": {
        "max_messages": {
          "description": "Maximum number of messages to accumulate before flushing immediately.",
          "type": "integer",
          "format": "uint",
          "minimum": 0
        },
        "max_delay_ms": {
          "description": "Maximum time to wait before flushing a non-full buffer.",
          "type": "integer",
          "format": "uint64",
          "minimum": 0
        }
      },
      "additionalProperties": false,
      "required": [
        "max_messages",
        "max_delay_ms"
      ]
    },
    "CookieJarMiddleware": {
      "description": "Cookie/session jar middleware configuration.\n\nOptimized for HTTP by default: it can read `cookie` and `set-cookie` metadata,\npersist session cookies, and inject them into later outgoing requests.\n\nThe middleware can also capture arbitrary metadata values into the same session store\nand optionally expose stored values back into message metadata.",
      "type": "object",
      "properties": {
        "shared_scope": {
          "description": "Optional shared scope name. When set, middleware instances using the same scope\nshare one session store across endpoints/routes in the process.",
          "type": [
            "string",
            "null"
          ],
          "default": null
        },
        "cookie_metadata_key": {
          "description": "Metadata key used to read/write HTTP Cookie headers. Defaults to `cookie`.",
          "type": "string",
          "default": "cookie"
        },
        "set_cookie_metadata_key": {
          "description": "Metadata key used to read HTTP Set-Cookie responses. Defaults to `set-cookie`.",
          "type": "string",
          "default": "set-cookie"
        },
        "capture_metadata_keys": {
          "description": "Additional metadata keys to persist into the session value store.",
          "type": "array",
          "items": {
            "type": "string"
          },
          "default": []
        },
        "export_metadata_prefix": {
          "description": "Optional metadata prefix used to export stored values back onto each message.\n\nExported keys use `PREFIXcookie.<name>` for cookies and `PREFIXvalue.<name>` for\ncaptured generic values.",
          "type": [
            "string",
            "null"
          ],
          "default": null
        },
        "inject_metadata": {
          "description": "Optional mapping of outgoing metadata keys to stored session value names.\n\nExample: `{ \"authorization\": \"access_token\" }` copies the stored value\n`access_token` into outgoing metadata key `authorization` when not already present.",
          "type": "object",
          "additionalProperties": {
            "type": "string"
          },
          "default": {}
        }
      },
      "additionalProperties": false
    },
    "TransformMiddleware": {
      "description": "JSON transform middleware configuration.\n\nReshapes JSON payloads declaratively, in two stages over a single parse: field\n`mapping` (rename/move/nest), then `schema` (type coercion, defaults, validation).\nEither stage may be omitted; with neither configured the message passes through\nuntouched and is never parsed.\n\nOn an output endpoint a rejected message becomes a non-retryable failure, so a `dlq`\nmiddleware listed *after* this one captures it (publisher middlewares are wrapped in\nlist order, so the last entry is the outermost layer). On an input endpoint a rejected\nmessage is dropped from the batch and acknowledged, which is how invalid input is kept\nout of the route.",
      "type": "object",
      "properties": {
        "mapping": {
          "description": "Output field name -> source path (e.g. `firstName: \"$.first_name\"`). Dots nest the output.",
          "type": "object",
          "additionalProperties": {
            "$ref": "#/$defs/MappingRule"
          }
        },
        "schema": {
          "description": "Inline JSON Schema subset (type, properties, required, default, items, nullable, enum)."
        },
        "schema_file": {
          "description": "Path to a JSON Schema file. Read once at startup; never re-read per message.",
          "type": [
            "string",
            "null"
          ]
        },
        "coerce": {
          "description": "Coerce safely convertible types (e.g. `\"42\"` -> `42`) instead of rejecting. Defaults to true.",
          "type": "boolean",
          "default": true
        },
        "apply_defaults": {
          "description": "Insert `default` values from the schema for missing fields. Defaults to true.",
          "type": "boolean",
          "default": true
        },
        "coerce_empty_as_null": {
          "description": "Read an empty string as `null`, so a nullable field or its default wins. Defaults to false.",
          "type": "boolean",
          "default": false
        },
        "on_error": {
          "description": "What to do with a message that fails to transform. Defaults to `reject`.",
          "$ref": "#/$defs/TransformErrorPolicy",
          "default": "reject"
        }
      },
      "additionalProperties": false,
      "not": {
        "properties": {
          "schema_file": {
            "type": "string"
          }
        },
        "required": [
          "schema",
          "schema_file"
        ]
      }
    },
    "MappingRule": {
      "description": "How one output field is produced from the input document.\n\nEither a bare path string (`\"$.first_name\"`) or an object with a `path` plus an\noptional `default` and `required` flag.",
      "anyOf": [
        {
          "description": "Shorthand: just the source path.",
          "type": "string"
        },
        {
          "description": "Full form with a fallback value and/or a presence requirement.",
          "$ref": "#/$defs/DetailedMappingRule"
        }
      ]
    },
    "DetailedMappingRule": {
      "description": "Full mapping form with a fallback value and/or a presence requirement.",
      "type": "object",
      "properties": {
        "path": {
          "description": "Source path in the input document (e.g. `$.user.id`, `user.id`, `$.items[0]`).",
          "type": "string"
        },
        "default": {
          "description": "Value used when the source path is absent."
        },
        "required": {
          "description": "Reject the message when the source path is absent and no `default` is set.",
          "type": "boolean",
          "default": false
        }
      },
      "additionalProperties": false,
      "required": [
        "path"
      ]
    },
    "TransformErrorPolicy": {
      "description": "Action taken on a message that fails to transform.",
      "oneOf": [
        {
          "description": "Reject the message: non-retryable failure on output, dropped from the batch on input.",
          "type": "string",
          "const": "reject"
        },
        {
          "description": "Forward the original payload unchanged with the error recorded in metadata.",
          "type": "string",
          "const": "pass_through"
        }
      ]
    },
    "EncryptionConfig": {
      "description": "AEAD encryption settings, shared by the `encryption` middleware (per-message\npayload encryption) and the at-rest `encryption` field of the file and\nobject_store endpoints. Requires the `encryption` feature.",
      "type": "object",
      "properties": {
        "cipher": {
          "description": "AEAD cipher. Defaults to `xchacha20poly1305`.",
          "$ref": "#/$defs/CipherKind",
          "default": "xchacha20poly1305"
        },
        "key_id": {
          "description": "Key identifier written into each envelope; selects the key when decrypting. Defaults to `default`.",
          "type": "string",
          "default": "default"
        },
        "key": {
          "description": "Base64-encoded 32-byte key. Supports `${env:VAR}` to read it from the environment.",
          "type": "string",
          "format": "password"
        },
        "decrypt_keys": {
          "description": "Extra `key_id -> base64 key` entries accepted when decrypting (key rotation).",
          "type": "object",
          "format": "password",
          "additionalProperties": {
            "type": "string"
          }
        }
      },
      "additionalProperties": false,
      "required": [
        "key"
      ]
    },
    "CipherKind": {
      "description": "AEAD cipher selection for [`EncryptionConfig`].",
      "oneOf": [
        {
          "description": "XChaCha20-Poly1305 (default): 192-bit random nonce, safe at high message rates.",
          "type": "string",
          "const": "xchacha20poly1305"
        },
        {
          "description": "AES-256-GCM: 96-bit random nonce; prefer the default for very high volumes.",
          "type": "string",
          "const": "aes256gcm"
        }
      ]
    },
    "CompressionMiddleware": {
      "description": "Payload-compression middleware configuration.\n\nCompresses each message payload on the output side and decompresses it on the input\nside; metadata and routing keys are left untouched. Requires the `compression` feature.\nDistinct from the `file`/`object_store` batch `compression` field, which stays\nCLI-decodable at rest — this operates per message on any transport.",
      "type": "object",
      "properties": {
        "algorithm": {
          "description": "Algorithm: `none`, `gzip`, `lz4`, or `zstd`. Defaults to `zstd`.",
          "$ref": "#/$defs/Compression",
          "default": "zstd"
        },
        "max_decompressed_bytes": {
          "description": "Reject a decompressed payload larger than this many bytes (decompression-bomb guard).\nConsumer side only; unset means no limit.",
          "type": [
            "integer",
            "null"
          ],
          "format": "uint64",
          "minimum": 0,
          "default": null
        }
      },
      "additionalProperties": false
    },
    "Compression": {
      "description": "Compression algorithm. Used for at-rest batches (file, object_store) and for HTTP\nbody compression (http, clickhouse). Orthogonal to `format`.",
      "oneOf": [
        {
          "description": "No compression (default).",
          "type": "string",
          "const": "none"
        },
        {
          "description": "gzip; each batch is a self-contained member, so files stay readable with `zcat`.",
          "type": "string",
          "const": "gzip"
        },
        {
          "description": "lz4 frame format; each batch is a self-contained frame (`lz4 -d` compatible).",
          "type": "string",
          "const": "lz4"
        },
        {
          "description": "zstd; each batch is a self-contained frame, concatenated frames decode as one\nstream (`zstd -d` compatible). Better ratio than lz4, still fast.",
          "type": "string",
          "const": "zstd"
        }
      ]
    },
    "AwsConfig": {
      "type": "object",
      "properties": {
        "queue_url": {
          "description": "The SQS queue URL. Required for Consumer. Optional for Publisher if `topic_arn` is set. If it contains userinfo, it will be treated as a secret.",
          "type": [
            "string",
            "null"
          ],
          "format": "password"
        },
        "topic_arn": {
          "description": "(Publisher only) The SNS topic ARN.",
          "type": [
            "string",
            "null"
          ]
        },
        "region": {
          "description": "AWS Region (e.g., \"us-east-1\").",
          "type": [
            "string",
            "null"
          ]
        },
        "endpoint_url": {
          "description": "Custom endpoint URL (e.g., for LocalStack).",
          "type": [
            "string",
            "null"
          ],
          "format": "password"
        },
        "access_key": {
          "description": "AWS Access Key ID.",
          "type": [
            "string",
            "null"
          ],
          "format": "password"
        },
        "secret_key": {
          "description": "AWS Secret Access Key.",
          "type": [
            "string",
            "null"
          ],
          "format": "password"
        },
        "session_token": {
          "description": "AWS Session Token.",
          "type": [
            "string",
            "null"
          ],
          "format": "password"
        },
        "max_messages": {
          "description": "(Consumer only) Maximum number of messages to receive in a batch (1-10).",
          "type": [
            "integer",
            "null"
          ],
          "format": "int32",
          "minimum": 1,
          "maximum": 10
        },
        "wait_time_seconds": {
          "description": "(Consumer only) Wait time for long polling in seconds (0-20).",
          "type": [
            "integer",
            "null"
          ],
          "format": "int32",
          "minimum": 0,
          "maximum": 20
        },
        "binary_payload_mode": {
          "description": "Use binary payloads in SQS/SNS messages.",
          "type": "boolean",
          "default": false
        }
      },
      "additionalProperties": false
    },
    "KafkaConfig": {
      "description": "General Kafka connection configuration.",
      "type": "object",
      "properties": {
        "url": {
          "description": "Comma-separated list of Kafka broker URLs. If it contains userinfo, it will be treated as a secret.",
          "type": "string",
          "format": "password"
        },
        "topic": {
          "description": "The Kafka topic to produce to or consume from.",
          "type": [
            "string",
            "null"
          ]
        },
        "username": {
          "description": "Optional username for SASL authentication.",
          "type": [
            "string",
            "null"
          ]
        },
        "password": {
          "description": "Optional password for SASL authentication.",
          "type": [
            "string",
            "null"
          ],
          "format": "password"
        },
        "tls": {
          "description": "TLS configuration.",
          "$ref": "#/$defs/TlsConfig",
          "default": {
            "required": false,
            "ca_file": null,
            "cert_file": null,
            "key_file": null,
            "cert_password": null,
            "accept_invalid_certs": false
          }
        },
        "group_id": {
          "description": "(Consumer only) Consumer group ID.\nIf not provided, the consumer acts in **Subscriber mode**: it generates a unique, ephemeral group ID and starts consuming from the latest offset.",
          "type": [
            "string",
            "null"
          ]
        },
        "source_metadata": {
          "description": "(Consumer only) Include authoritative `mqb.src.kafka_*` source positions. Defaults to false.",
          "type": "boolean",
          "default": false
        },
        "delayed_ack": {
          "description": "(Publisher only) If true, do not wait for an acknowledgement when sending to broker. Defaults to false.",
          "type": "boolean",
          "default": false
        },
        "producer_options": {
          "description": "(Publisher only) Additional librdkafka producer configuration options (key-value pairs).",
          "type": [
            "array",
            "null"
          ],
          "items": {
            "type": "array",
            "prefixItems": [
              {
                "type": "string"
              },
              {
                "type": "string"
              }
            ],
            "minItems": 2,
            "maxItems": 2
          },
          "default": null
        },
        "consumer_options": {
          "description": "(Consumer only) Additional librdkafka consumer configuration options (key-value pairs).",
          "type": [
            "array",
            "null"
          ],
          "items": {
            "type": "array",
            "prefixItems": [
              {
                "type": "string"
              },
              {
                "type": "string"
              }
            ],
            "minItems": 2,
            "maxItems": 2
          },
          "default": null
        },
        "shared": {
          "description": "(Publisher only) Share one producer per connection (default: true); false gives a dedicated producer.",
          "type": [
            "boolean",
            "null"
          ],
          "default": true
        },
        "partitions": {
          "description": "(Publisher only) Partition count used when auto-creating the topic (default: 6).\nHigher values raise write/consume parallelism; ordering is only guaranteed per\npartition key (message_id), not across the whole topic. Ignored if the topic exists.",
          "type": [
            "integer",
            "null"
          ],
          "format": "int32",
          "minimum": 1,
          "default": 6
        },
        "partition_key": {
          "description": "(Publisher only) Name of a metadata field whose value is used as the Kafka record\nkey (drives partitioning/ordering). Unset, or absent on a given message, falls back\nto the message id. Default unset.",
          "type": [
            "string",
            "null"
          ],
          "default": null
        }
      },
      "additionalProperties": false,
      "required": [
        "url"
      ]
    },
    "TlsConfig": {
      "description": "TLS configuration for secure connections.\n\nConfigures Transport Layer Security (TLS/SSL) for encrypted communication.\nSupports both client certificate (mutual TLS) and server certificate validation.\n\n# Examples\n\n```\nuse mq_bridge::models::TlsConfig;\n\nlet tls = TlsConfig {\n    required: true,\n    ca_file: Some(\"/path/to/ca.pem\".to_string()),\n    cert_file: Some(\"/path/to/cert.pem\".to_string()),\n    key_file: Some(\"/path/to/key.pem\".to_string()),\n    ..Default::default()\n};\n```",
      "type": "object",
      "properties": {
        "required": {
          "description": "If true, enable TLS/SSL.",
          "type": "boolean",
          "default": false
        },
        "ca_file": {
          "description": "Path to the CA certificate file.",
          "type": [
            "string",
            "null"
          ]
        },
        "cert_file": {
          "description": "Path to the client certificate file (PEM).",
          "type": [
            "string",
            "null"
          ]
        },
        "key_file": {
          "description": "Path to the client private key file (PEM).",
          "type": [
            "string",
            "null"
          ]
        },
        "cert_password": {
          "description": "Password for the private key (if encrypted).",
          "type": [
            "string",
            "null"
          ],
          "format": "password"
        },
        "accept_invalid_certs": {
          "description": "If true, disable server certificate verification (insecure).",
          "type": "boolean",
          "default": false
        }
      },
      "additionalProperties": false
    },
    "NatsConfig": {
      "description": "General NATS connection configuration.",
      "type": "object",
      "properties": {
        "url": {
          "description": "Comma-separated list of NATS server URLs (e.g., \"nats://localhost:4222,nats://localhost:4223\"). If it contains userinfo, it will be treated as a secret.",
          "type": "string",
          "format": "password"
        },
        "subject": {
          "description": "The NATS subject to publish to or subscribe to. If a stream is\nauto-created, it's scoped to `{stream}.>`, so prefix accordingly.",
          "type": [
            "string",
            "null"
          ]
        },
        "stream": {
          "description": "The JetStream stream name. Required for Consumers, even with\n`no_jetstream: true` (unused there, but still validated).",
          "type": [
            "string",
            "null"
          ]
        },
        "username": {
          "description": "Optional username for authentication.",
          "type": [
            "string",
            "null"
          ]
        },
        "password": {
          "description": "Optional password for authentication.",
          "type": [
            "string",
            "null"
          ],
          "format": "password"
        },
        "tls": {
          "description": "TLS configuration.",
          "$ref": "#/$defs/TlsConfig",
          "default": {
            "required": false,
            "ca_file": null,
            "cert_file": null,
            "key_file": null,
            "cert_password": null,
            "accept_invalid_certs": false
          }
        },
        "token": {
          "description": "Optional token for authentication.",
          "type": [
            "string",
            "null"
          ],
          "format": "password"
        },
        "request_reply": {
          "description": "(Publisher only) If true, the publisher uses the request-reply pattern.\nIt sends a request and waits for a response (using `core_client.request_with_headers()`).\nDefaults to false.",
          "type": "boolean",
          "default": false
        },
        "request_timeout_ms": {
          "description": "(Publisher only) Timeout for request-reply operations in milliseconds. Defaults to 30000ms.",
          "type": [
            "integer",
            "null"
          ],
          "format": "uint64",
          "minimum": 0
        },
        "delayed_ack": {
          "description": "(Publisher only) If true, do not wait for an acknowledgement when sending to broker. Defaults to false.",
          "type": "boolean",
          "default": false
        },
        "deduplicate": {
          "description": "(Publisher only, JetStream) If true, publish a `Nats-Msg-Id` header (from the message id) so\nJetStream deduplicates redeliveries within the stream's duplicate window. Defaults to false.",
          "type": "boolean",
          "default": false
        },
        "no_jetstream": {
          "description": "If no_jetstream: true, use Core NATS (fire-and-forget) instead of JetStream. Defaults to false.",
          "type": "boolean",
          "default": false
        },
        "subscriber_mode": {
          "description": "(Consumer only) If true, use ephemeral **Subscriber mode**. Defaults to false (durable consumer).",
          "type": "boolean",
          "default": false
        },
        "source_metadata": {
          "description": "(Consumer only) Include authoritative `mqb.src.nats_*` source positions. Defaults to false.",
          "type": "boolean",
          "default": false
        },
        "stream_max_messages": {
          "description": "(Publisher only) Maximum number of messages in the stream (if created by the bridge). Defaults to 1,000,000.",
          "type": [
            "integer",
            "null"
          ],
          "format": "int64"
        },
        "deliver_policy": {
          "description": "(Consumer only) The delivery policy for the consumer. Defaults to \"all\".",
          "anyOf": [
            {
              "$ref": "#/$defs/NatsDeliverPolicy"
            },
            {
              "type": "null"
            }
          ]
        },
        "stream_max_bytes": {
          "description": "(Publisher only) Maximum total bytes in the stream (if created by the bridge). Defaults to 1GB.",
          "type": [
            "integer",
            "null"
          ],
          "format": "int64"
        },
        "prefetch_count": {
          "description": "(Consumer only) Number of messages to prefetch from the consumer. Defaults to 10000.",
          "type": [
            "integer",
            "null"
          ],
          "format": "uint",
          "minimum": 0
        },
        "shared": {
          "description": "Share one NATS client per connection (default: true); false forces a dedicated connection.",
          "type": [
            "boolean",
            "null"
          ],
          "default": true
        }
      },
      "additionalProperties": false,
      "required": [
        "url"
      ]
    },
    "NatsDeliverPolicy": {
      "type": "string",
      "enum": [
        "all",
        "last",
        "new",
        "last_per_subject"
      ]
    },
    "FileConfig": {
      "type": "object",
      "properties": {
        "path": {
          "description": "Path to the file, or to the directory holding the part files under `source_position` naming.",
          "type": "string"
        },
        "name_by": {
          "description": "(Sink only) `write_time` (default here, appends to `path`) or `source_position` (replay-safe part files, `path` is their directory).",
          "$ref": "#/$defs/NameBy",
          "default": "auto"
        },
        "idempotency": {
          "description": "Deprecated: use `name_by`. true = `source_position`, false = `write_time`; ignored when `name_by` is set.",
          "type": [
            "boolean",
            "null"
          ]
        },
        "delimiter": {
          "description": "Optional delimiter for messages. Defaults to newline (\"\\n\").\nCan be a string or a hex sequence (e.g. \"0x00\").\nCurrently only single-byte delimiters are supported.",
          "type": [
            "string",
            "null"
          ]
        },
        "format": {
          "description": "The format for writing messages to the file (Publisher) or interpreting them (Consumer). Defaults to `normal`.",
          "$ref": "#/$defs/FileFormat",
          "default": "normal"
        },
        "compression": {
          "description": "Per-batch compression (`none`, `gzip`, `lz4`, `zstd`). Requires the `compression` feature. Publishers: always. Consumers: must match, and only the default `consume` mode reads it.",
          "$ref": "#/$defs/Compression",
          "default": "none"
        },
        "encryption": {
          "description": "At-rest AEAD encryption applied after compression. Requires the `encryption` feature. Publishers: always. Consumers: must match, and only the default `consume` mode reads it.",
          "anyOf": [
            {
              "$ref": "#/$defs/EncryptionConfig"
            },
            {
              "type": "null"
            }
          ],
          "default": null
        },
        "source_metadata": {
          "description": "(Consumer only) Include authoritative `mqb.src.file_*` source positions; only `consume` mode reproduces them across restarts. Defaults to false.",
          "type": "boolean",
          "default": false
        }
      },
      "required": [
        "path"
      ],
      "anyOf": [
        {
          "oneOf": [
            {
              "description": "**Queue Mode**: Standard point-to-point consumption. Reads from the start\nof the file. If `delete` is true, processed lines are physically removed\nfrom the file once they are successfully acknowledged.",
              "type": "object",
              "properties": {
                "delete": {
                  "description": "If true, processed lines are physically removed from the file once\nthey are successfully acknowledged.",
                  "type": "boolean",
                  "default": false
                },
                "mode": {
                  "type": "string",
                  "const": "consume"
                }
              },
              "required": [
                "mode"
              ]
            },
            {
              "description": "**Broadcast Mode**: Pub-sub style consumption. Tails the file by starting\nat the current end. If `delete` is true, lines are removed only after\nall local application subscribers for this specific file have acknowledged them.",
              "type": "object",
              "properties": {
                "delete": {
                  "description": "If true, lines are removed only after all local application\nsubscribers for this file have acknowledged them.",
                  "type": "boolean",
                  "default": false
                },
                "mode": {
                  "type": "string",
                  "const": "subscribe"
                }
              },
              "required": [
                "mode"
              ]
            },
            {
              "description": "**Persistent Mode**: Consumption with external offset tracking.\nSaves the last read byte position to a `.offset` file identified by the `group_id`.\nThis allows the consumer to resume exactly where it left off after a restart\nwithout deleting data or requiring the bridge to stay running.",
              "type": "object",
              "properties": {
                "group_id": {
                  "description": "The consumer group ID that is used for offset tracking. Should be unique.",
                  "type": "string"
                },
                "read_from_tail": {
                  "description": "If true, starts reading from the end of the file if no offset is stored.\nIf false, starts reading from the beginning.",
                  "type": "boolean",
                  "default": false
                },
                "mode": {
                  "type": "string",
                  "const": "group_subscribe"
                }
              },
              "required": [
                "mode",
                "group_id"
              ]
            }
          ]
        },
        {}
      ]
    },
    "NameBy": {
      "description": "How a `file` or `object_store` sink names what it writes.\n\nThe name decides everything downstream: a name derived from the source position sorts by\nsource order and repeats exactly on a replay (so a re-write is a no-op), while a name\nderived from the write time sorts by write order and is unique per write.\n\n`auto` resolves against the input for `object_store` only. There the two schemes write the\nsame thing under a different name, so the choice is free; on `file` they are different sink\nstructures — one appended file versus a directory of part files — and deriving that from the\ninput would change what `path` means without the operator asking for it.",
      "oneOf": [
        {
          "description": "object_store: `source_position` when the input carries one, else `write_time`. file: always `write_time`.",
          "type": "string",
          "const": "auto"
        },
        {
          "description": "`part-<source>-<start>-<end>.<ext>`. Needs a replayable input; on `file` this turns `path` into a directory.",
          "type": "string",
          "const": "source_position"
        },
        {
          "description": "object_store: `<uuidv7>.<ext>` under an optional `YYYY/MM/DD/` prefix. file: appends to `path`.",
          "type": "string",
          "const": "write_time"
        }
      ]
    },
    "FileFormat": {
      "description": "Format for messages written to or read from a file.",
      "oneOf": [
        {
          "description": "The full `CanonicalMessage` is serialized to JSON. Payload is either base64 or utf8 text.",
          "type": "string",
          "const": "normal"
        },
        {
          "description": "The full `CanonicalMessage` is serialized to JSON. Payload is rendered as a JSON value if possible.",
          "type": "string",
          "const": "json"
        },
        {
          "description": "The full `CanonicalMessage` is serialized to JSON. Payload is rendered as a string if possible.",
          "type": "string",
          "const": "text"
        },
        {
          "description": "The raw payload of the message is written. For consumers, the line is read as raw bytes.",
          "type": "string",
          "const": "raw"
        },
        {
          "description": "CSV rows mapped to/from JSON objects (string values only). The first row is the header/schema.",
          "type": "string",
          "const": "csv"
        }
      ]
    },
    "ObjectStoreConfig": {
      "description": "Configuration for a cloud object-store endpoint (S3, GCS, Azure Blob, R2, ...).\n\nAs a **sink**, each flushed batch is written as one immutable object under `url`,\nnamed `<prefix>/[YYYY/MM/DD/]<uuidv7>.<ext>`. As a **source**, objects under `url`\nare listed in key order, fetched, split by `delimiter`, and emitted as messages;\nprogress is persisted to `checkpoint_store` (the last processed object key) so a\nrestart resumes without re-emitting. Objects are never mutated or deleted in place.",
      "type": "object",
      "properties": {
        "url": {
          "description": "Object-store URL, e.g. `s3://bucket/prefix`, `gs://bucket/prefix`,\n`az://account/container/prefix`. Credentials are resolved from the environment by\nthe `object_store` crate (same mechanism as the checkpoint backend); R2 uses\n`s3://` plus a custom `AWS_ENDPOINT_URL`.",
          "type": "string"
        },
        "name_by": {
          "description": "(Sink only) `auto`, `write_time` (uuidv7 name) or `source_position` (name carries the source range).",
          "$ref": "#/$defs/NameBy",
          "default": "auto"
        },
        "idempotency": {
          "description": "Deprecated: use `name_by`. true = `source_position`, false = `write_time`; ignored when `name_by` is set.",
          "type": [
            "boolean",
            "null"
          ]
        },
        "format": {
          "description": "Record encoding within an object, shared with the file endpoint. Defaults to\n`normal` (one JSON `CanonicalMessage` per line). CSV is supported for sources only.",
          "$ref": "#/$defs/FileFormat",
          "default": "normal"
        },
        "delimiter": {
          "description": "Record delimiter within an object. Defaults to newline (\"\\n\"). Can be a string or a\nhex sequence (e.g. \"0x00\").",
          "type": [
            "string",
            "null"
          ]
        },
        "checkpoint_store": {
          "description": "(Source only) Durable resume store URL recording the last processed object key, e.g.\n`file:///var/lib/mqb/obj.json`, `s3://bucket/cursors`, or `postgres://…`. Without it\nevery restart re-lists and re-emits all objects.",
          "type": [
            "string",
            "null"
          ]
        },
        "cursor_id": {
          "description": "(Source only) Cursor id namespacing the checkpoint key; enables durable resume.",
          "type": [
            "string",
            "null"
          ]
        },
        "polling_interval_ms": {
          "description": "(Source only) Idle poll interval in milliseconds when no new objects are found.\nDefaults to 1000.",
          "type": [
            "integer",
            "null"
          ],
          "format": "uint64",
          "minimum": 0
        },
        "max_object_bytes": {
          "description": "(Source only) Maximum size in bytes of a single object to fetch into memory. An object\nlarger than this fails the read (surfaced as a consumer error) instead of being\nbuffered whole. Unset means no limit (the whole object is materialized).",
          "type": [
            "integer",
            "null"
          ],
          "format": "uint64",
          "minimum": 0
        },
        "date_partition": {
          "description": "(Sink only) Prepend a `YYYY/MM/DD/` path (write time, UTC) to each object key. Applies to\n`write_time` naming only; defaults to on. Purely for readability / lifecycle rules.",
          "type": [
            "boolean",
            "null"
          ],
          "default": null
        },
        "extension": {
          "description": "(Sink only) Extension for written objects, without the dot. Defaults to a value derived\nfrom `format`, `compression` and `encryption` (e.g. `jsonl`, `csv`, `bin`, `jsonl.gz`,\n`jsonl.lz4`, `jsonl.gz.enc`); encrypted objects get a trailing `.enc` since they are\nciphertext, not a directly decompressible `.gz`.",
          "type": [
            "string",
            "null"
          ]
        },
        "compression": {
          "description": "Whole-object compression (`none`, `gzip`, `lz4`, `zstd`). Requires the `compression` feature.",
          "$ref": "#/$defs/Compression",
          "default": "none"
        },
        "encryption": {
          "description": "At-rest AEAD encryption applied after compression. Requires the `encryption` feature.",
          "anyOf": [
            {
              "$ref": "#/$defs/EncryptionConfig"
            },
            {
              "type": "null"
            }
          ],
          "default": null
        }
      },
      "required": [
        "url"
      ]
    },
    "StaticConfig": {
      "description": "Configuration for the `static` endpoint. Accepts either a bare string (the response body, JSON-encoded for backward compatibility) or a map where only `body` is required and `raw` / `metadata` are optional.",
      "oneOf": [
        {
          "description": "The response body, JSON-encoded as a string.",
          "type": "string"
        },
        {
          "type": "object",
          "properties": {
            "body": {
              "description": "The static response body.",
              "type": "string"
            },
            "raw": {
              "description": "Send the body verbatim instead of JSON-encoding it as a string.",
              "type": "boolean",
              "default": false
            },
            "metadata": {
              "description": "Extra metadata entries attached to the produced message.",
              "type": "object",
              "additionalProperties": {
                "type": "string"
              }
            }
          },
          "required": [
            "body"
          ],
          "additionalProperties": false
        }
      ]
    },
    "MemoryConfig": {
      "type": "object",
      "properties": {
        "topic": {
          "description": "The topic name or transport URL. Can be:\n- Simple name: \"my-topic\" (defaults to memory://my-topic)\n- Memory URL: \"memory://my-topic\"\n- IPC URL: \"ipc://my-queue\" or \"ipc:///path/to/socket\"\n- Unix socket: \"unix:///path/to/socket\" (Unix only)\n- Named pipe: \"pipe://my-pipe\" (Windows only)\n\nEither `topic` or `url` can be specified (they are serde aliases).",
          "type": "string",
          "minLength": 1
        },
        "capacity": {
          "description": "The capacity of the channel. Defaults to 100.",
          "type": [
            "integer",
            "null"
          ],
          "format": "uint",
          "minimum": 0
        },
        "request_reply": {
          "description": "(Publisher only) If true, send() waits for a response.",
          "type": "boolean",
          "default": false
        },
        "request_timeout_ms": {
          "description": "(Publisher only) Timeout for request-reply operations in milliseconds. Defaults to 30000ms.",
          "type": [
            "integer",
            "null"
          ],
          "format": "uint64",
          "minimum": 0
        },
        "subscribe_mode": {
          "description": "(Consumer only) If true, act as a **Subscriber** (fan-out). Defaults to false (queue).",
          "type": "boolean",
          "default": false
        },
        "enable_nack": {
          "description": "(Consumer only) If true, enables NACK support (re-queuing), which requires cloning messages.\nDefaults to false for memory:// transports, automatically true for IPC transports (ipc://, unix://, pipe://).",
          "type": "boolean",
          "default": false
        },
        "url": {
          "description": "Alias for `topic`. Use either `topic` or `url`.",
          "type": "string",
          "minLength": 1
        }
      },
      "additionalProperties": false,
      "anyOf": [
        {
          "required": [
            "topic"
          ]
        },
        {
          "required": [
            "url"
          ]
        }
      ]
    },
    "SledConfig": {
      "description": "General Sled database configuration",
      "type": "object",
      "properties": {
        "path": {
          "description": "Path to the Sled database directory.",
          "type": "string"
        },
        "tree": {
          "description": "The tree name to use as a queue. Defaults to \"default\".",
          "type": [
            "string",
            "null"
          ]
        },
        "read_from_start": {
          "description": "(Consumer only) If true, start reading from the beginning of the tree.",
          "type": "boolean",
          "default": false
        },
        "delete_after_read": {
          "description": "(Consumer only) If true, delete messages after processing (Queue mode).",
          "type": "boolean",
          "default": false
        }
      },
      "additionalProperties": false,
      "required": [
        "path"
      ]
    },
    "AmqpConfig": {
      "description": "General AMQP connection configuration.",
      "type": "object",
      "properties": {
        "url": {
          "description": "AMQP connection URI. The `lapin` client connects to a single host specified in the URI. If it contains userinfo, it will be treated as a secret.\nFor high availability, provide the address of a load balancer or use DNS resolution\nthat points to multiple brokers. Example: \"amqp://localhost:5672/vhost\".",
          "type": "string",
          "format": "password"
        },
        "queue": {
          "description": "The AMQP queue name.",
          "type": [
            "string",
            "null"
          ]
        },
        "subscribe_mode": {
          "description": "(Consumer only) If true, act as a **Subscriber** (fan-out). Defaults to false.",
          "type": "boolean",
          "default": false
        },
        "source_metadata": {
          "description": "(Consumer only) Include authoritative `mqb.src.amqp_*` source positions. Defaults to false.",
          "type": "boolean",
          "default": false
        },
        "username": {
          "description": "Optional username for authentication.",
          "type": [
            "string",
            "null"
          ]
        },
        "password": {
          "description": "Optional password for authentication.",
          "type": [
            "string",
            "null"
          ],
          "format": "password"
        },
        "tls": {
          "description": "TLS configuration.",
          "$ref": "#/$defs/TlsConfig",
          "default": {
            "required": false,
            "ca_file": null,
            "cert_file": null,
            "key_file": null,
            "cert_password": null,
            "accept_invalid_certs": false
          }
        },
        "exchange": {
          "description": "The exchange to publish to or bind the queue to.",
          "type": [
            "string",
            "null"
          ]
        },
        "prefetch_count": {
          "description": "(Consumer only) Number of messages to prefetch. Defaults to 100.",
          "type": [
            "integer",
            "null"
          ],
          "format": "uint16",
          "minimum": 0,
          "maximum": 65535
        },
        "no_persistence": {
          "description": "If true, declare queues as non-durable (transient). Defaults to false. Affects both Consumer (queue durability) and Publisher (message persistence).",
          "type": "boolean",
          "default": false
        },
        "no_declare_queue": {
          "description": "(Publisher only) If true, do not attempt to declare the queue. Assumes the queue already exists. Defaults to false.",
          "type": "boolean",
          "default": false
        },
        "delayed_ack": {
          "description": "(Publisher only) If true, do not wait for an acknowledgement when sending to broker. Defaults to false.",
          "type": "boolean",
          "default": false
        }
      },
      "additionalProperties": false,
      "required": [
        "url"
      ]
    },
    "MongoDbConfig": {
      "description": "General MongoDB connection configuration.",
      "type": "object",
      "properties": {
        "url": {
          "description": "MongoDB connection string URI. Can contain a comma-separated list of hosts for a replica set. If it contains userinfo, it will be treated as a secret.\nCredentials provided via the separate `username` and `password` fields take precedence over any credentials embedded in the URL.",
          "type": "string",
          "format": "password"
        },
        "collection": {
          "description": "The MongoDB collection name.",
          "type": [
            "string",
            "null"
          ]
        },
        "username": {
          "description": "Optional username. Takes precedence over any credentials embedded in the `url`.\nUse embedded URL credentials for simple one-off connections but prefer explicit username/password fields (or environment-sourced secrets) for clarity and secret management in production.",
          "type": [
            "string",
            "null"
          ]
        },
        "password": {
          "description": "Optional password. Takes precedence over any credentials embedded in the `url`.\nUse embedded URL credentials for simple one-off connections but prefer explicit username/password fields (or environment-sourced secrets) for clarity and secret management in production.",
          "type": [
            "string",
            "null"
          ],
          "format": "password"
        },
        "tls": {
          "description": "TLS configuration.",
          "$ref": "#/$defs/TlsConfig",
          "default": {
            "required": false,
            "ca_file": null,
            "cert_file": null,
            "key_file": null,
            "cert_password": null,
            "accept_invalid_certs": false
          }
        },
        "database": {
          "description": "The database name.",
          "type": "string"
        },
        "polling_interval_ms": {
          "description": "(Consumer only) Polling interval in milliseconds for the consumer (when not using Change Streams). Defaults to 100ms.",
          "type": [
            "integer",
            "null"
          ],
          "format": "uint64",
          "minimum": 0
        },
        "reply_polling_ms": {
          "description": "(Publisher only) Polling interval in milliseconds for the publisher when waiting for a reply. Defaults to 50ms.",
          "type": [
            "integer",
            "null"
          ],
          "format": "uint64",
          "minimum": 0
        },
        "request_reply": {
          "description": "(Publisher only) If true, the publisher will wait for a response in a dedicated collection. Defaults to false.",
          "type": "boolean",
          "default": false
        },
        "consume": {
          "description": "(Consumer only) How to consume the collection: `capture_all` (default, read existing\ndocuments first, then watch for changes — non-destructive, for bulk reads and ETL),\n`capture_new` (watch an existing collection for changes only), `snapshot` (one-shot\nnon-destructive read that ends on drain, the option without a replica set), or\n`consumer` (competing-consumers work queue — destructive and intended for jobs).\nThe bridge selects the underlying mechanism automatically. If unset, the deprecated\n`change_stream` boolean is honored for backward compatibility.",
          "anyOf": [
            {
              "$ref": "#/$defs/MongoConsume"
            },
            {
              "type": "null"
            }
          ]
        },
        "receive_query": {
          "description": "(Consumer only) Optional custom MongoDB query to filter messages. Provided as a JSON string (e.g., '{\"type\": \"notification\"}').",
          "type": [
            "string",
            "null"
          ]
        },
        "source_metadata": {
          "description": "(Consumer only, `capture_new`/`capture_all`) Include authoritative `mqb.src.mongodb_*`\nsource positions. Defaults to false.",
          "type": "boolean",
          "default": false
        },
        "change_stream": {
          "description": "(Consumer only) **Deprecated** — use `consume: capture_new`. Kept for compatibility.",
          "type": "boolean",
          "default": false
        },
        "checkpoint_store": {
          "description": "(Consumer only) Where to persist the resume cursor in `capture_new`/`capture_all` mode. A URL\nselects the backend; a bare name (or `/name`) reuses the **source** database with that name:\n- absent → source database, collection `mqb_cursors_<source_collection>` (auto-unique)\n- `/my_cursors` → source database, collection `my_cursors`\n- `file:///var/lib/mqb/cursors.json` → local JSON file (read-only / write-restricted sources)\n- `mongodb://host/db/collection` → external MongoDB collection (collection optional)\n- `postgres://user@host/db/table` or `mysql://host/db/table` → external SQL table (table optional)\n- `s3://bucket/prefix` (also `gs://`, `az://`, `abfs://`) → cloud object store; creds via env\n\nWhen no collection/table is named, it defaults to `mqb_cursors_<source_collection>`.\nMay embed connection credentials, so it is treated as a secret.",
          "type": [
            "string",
            "null"
          ],
          "format": "password"
        },
        "request_timeout_ms": {
          "description": "(Publisher only) Timeout for request-reply operations in milliseconds. Defaults to 30000ms.",
          "type": [
            "integer",
            "null"
          ],
          "format": "uint64",
          "minimum": 0
        },
        "ttl_seconds": {
          "description": "(Publisher only) TTL in seconds for documents created by the publisher. If set, a TTL index is created.",
          "type": [
            "integer",
            "null"
          ],
          "format": "uint64",
          "minimum": 0
        },
        "capped_size_bytes": {
          "description": "(Publisher only) If set, creates a capped collection with this size in bytes.",
          "type": [
            "integer",
            "null"
          ],
          "format": "int64"
        },
        "format": {
          "description": "Format for storing messages. Defaults to Normal.",
          "$ref": "#/$defs/MongoDbFormat",
          "default": "normal"
        },
        "id_field": {
          "description": "(Publisher only) Top-level payload field whose value becomes the document `_id`, or a\nreplay-stable `${...}` template such as `${metadata:mqb.id}`. Enables idempotent inserts\nthrough MongoDB's unique `_id` index. Sink collections only.",
          "type": [
            "string",
            "null"
          ]
        },
        "report_outcome": {
          "description": "(Publisher only) Return the message with metadata `mongodb.outcome` = `inserted`/`existed`\n(dup-key) so a `request`+`switch` can branch. Sink collections only; pair with `id_field`.",
          "type": "boolean",
          "default": false
        },
        "cursor_id": {
          "description": "The ID used for the cursor in sequenced mode. If not provided, consumption starts from the current sequence (ephemeral).",
          "type": [
            "string",
            "null"
          ]
        },
        "meta_collection": {
          "description": "(Optional) Collection to store sequence counters and cursor positions. Defaults to the message collection if not set.",
          "type": [
            "string",
            "null"
          ]
        },
        "shared": {
          "description": "Share one MongoDB client per connection (default: true); false forces a dedicated client.",
          "type": [
            "boolean",
            "null"
          ],
          "default": true
        }
      },
      "additionalProperties": false,
      "required": [
        "url",
        "database"
      ]
    },
    "MongoConsume": {
      "description": "How a MongoDB endpoint consumes a collection. One intent-named selector — the bridge picks the\nunderlying mechanism (change stream vs. polling) automatically. Defaults to `capture_all`.",
      "oneOf": [
        {
          "description": "**Queue** — competing consumers: claim, process, delete, so each document goes to exactly one\nreader. Destructive and intended for jobs, not bulk reads.",
          "type": "string",
          "const": "consumer"
        },
        {
          "description": "**One-shot read** — page documents by `_id`, then end the route. Non-destructive and\nnon-resumable; needs no replica set and reads arbitrary collections. This is not a\npoint-in-time snapshot: separate page queries can observe concurrent inserts and deletes,\nwhile inserts below the current `_id` high-water mark can be missed.",
          "type": "string",
          "const": "snapshot"
        },
        {
          "description": "**Watch existing collection** — capture changes from now on (insert/update/delete), resuming\nunder `cursor_id`. Reads an existing collection non-destructively; never ends on drain.",
          "type": "string",
          "const": "capture_new"
        },
        {
          "description": "**Watch existing collection** — read the existing documents first, then capture changes.\nNon-destructive and the fastest read mode; use this for bulk reads and ETL. Default.\nNeeds a replica set; on a standalone `mongod` use `snapshot` or `consumer`.",
          "type": "string",
          "const": "capture_all"
        }
      ]
    },
    "MongoDbFormat": {
      "description": "MongoDB message storage format.\n\nDetermines how messages are stored and retrieved from MongoDB collections.",
      "type": "string",
      "enum": [
        "normal",
        "json",
        "text",
        "raw"
      ]
    },
    "MqttConfig": {
      "description": "General MQTT connection configuration.",
      "type": "object",
      "properties": {
        "url": {
          "description": "MQTT broker URL (e.g., \"tcp://localhost:1883\"). Does not support multiple hosts. If it contains userinfo, it will be treated as a secret.",
          "type": "string",
          "format": "password"
        },
        "topic": {
          "description": "The MQTT topic.",
          "type": [
            "string",
            "null"
          ]
        },
        "username": {
          "description": "Optional username for authentication.",
          "type": [
            "string",
            "null"
          ]
        },
        "password": {
          "description": "Optional password for authentication.",
          "type": [
            "string",
            "null"
          ],
          "format": "password"
        },
        "tls": {
          "description": "TLS configuration.",
          "$ref": "#/$defs/TlsConfig",
          "default": {
            "required": false,
            "ca_file": null,
            "cert_file": null,
            "key_file": null,
            "cert_password": null,
            "accept_invalid_certs": false
          }
        },
        "client_id": {
          "description": "Optional client ID. If not provided, one is generated or derived from route name.",
          "type": [
            "string",
            "null"
          ]
        },
        "queue_capacity": {
          "description": "Capacity of the internal channel for incoming messages. Defaults to 100.",
          "type": [
            "integer",
            "null"
          ],
          "format": "uint",
          "minimum": 0
        },
        "max_inflight": {
          "description": "Maximum number of inflight messages.",
          "type": [
            "integer",
            "null"
          ],
          "format": "uint16",
          "minimum": 0,
          "maximum": 65535
        },
        "qos": {
          "description": "Quality of Service level (0, 1, or 2). Defaults to 1.",
          "type": [
            "integer",
            "null"
          ],
          "format": "uint8",
          "minimum": 0,
          "maximum": 255
        },
        "clean_session": {
          "description": "(Consumer only) If true, start with a clean session. Defaults to false (persistent session). Setting this to true effectively enables **Subscriber mode** (ephemeral).",
          "type": "boolean",
          "default": false
        },
        "keep_alive_seconds": {
          "description": "Keep-alive interval in seconds. Defaults to 20.",
          "type": [
            "integer",
            "null"
          ],
          "format": "uint64",
          "minimum": 0
        },
        "protocol": {
          "description": "MQTT protocol version (V3 or V5). Defaults to V5.",
          "$ref": "#/$defs/MqttProtocol",
          "default": "v5"
        },
        "session_expiry_interval": {
          "description": "Session expiry interval in seconds (MQTT v5 only).",
          "type": [
            "integer",
            "null"
          ],
          "format": "uint32",
          "minimum": 0
        },
        "delayed_ack": {
          "description": "(Consumer only) If true, messages are acknowledged immediately upon receipt (auto-ack).\nIf false (default), messages are acknowledged after processing (manual-ack).\nNote: For QoS 1/2 the publisher always waits for end-to-end broker\nconfirmation (PUBACK/PUBCOMP) before reporting success, independent of\nthis setting; QoS 0 remains fire-and-forget.",
          "type": "boolean",
          "default": false
        }
      },
      "additionalProperties": false,
      "required": [
        "url"
      ]
    },
    "MqttProtocol": {
      "description": "MQTT protocol version.\n\nSpecifies which version of the MQTT protocol to use for connections.",
      "type": "string",
      "enum": [
        "v5",
        "v3"
      ]
    },
    "HttpConfig": {
      "description": "General HTTP connection configuration.",
      "type": "object",
      "properties": {
        "url": {
          "description": "For consumers, the listen address (e.g., \"0.0.0.0:8080\"). For publishers, the target URL.",
          "type": "string"
        },
        "path": {
          "description": "(Consumer only) Optional request path filter. If set, only requests whose URI path matches exactly are delivered to this consumer.",
          "type": [
            "string",
            "null"
          ]
        },
        "method": {
          "description": "(Optional) HTTP method. For publishers: the method to use (defaults to POST). For consumers: restrict to this method (others return 405).",
          "type": [
            "string",
            "null"
          ]
        },
        "tls": {
          "description": "TLS configuration.",
          "$ref": "#/$defs/TlsConfig",
          "default": {
            "required": false,
            "ca_file": null,
            "cert_file": null,
            "key_file": null,
            "cert_password": null,
            "accept_invalid_certs": false
          }
        },
        "workers": {
          "description": "(Consumer only) Number of worker threads to use. Defaults to 0 for unlimited.",
          "type": [
            "integer",
            "null"
          ],
          "format": "uint",
          "minimum": 0
        },
        "message_id_header": {
          "description": "(Consumer only) Header key to extract the message ID from. Defaults to \"message-id\".",
          "type": [
            "string",
            "null"
          ]
        },
        "request_timeout_ms": {
          "description": "Timeout for HTTP requests in milliseconds. For consumers, it's the request-reply timeout. For publishers, it's the timeout for each individual request. Defaults to 30000ms.",
          "type": [
            "integer",
            "null"
          ],
          "format": "uint64",
          "minimum": 0
        },
        "internal_buffer_size": {
          "description": "(Consumer only) Internal buffer size for the channel. Defaults to 100.",
          "type": [
            "integer",
            "null"
          ],
          "format": "uint",
          "minimum": 0
        },
        "fire_and_forget": {
          "description": "(Consumer only) If true, respond immediately with 202 Accepted without waiting for downstream processing. Defaults to false.",
          "type": "boolean",
          "default": false
        },
        "receive_streamable": {
          "description": "(Consumer only) If true, read request bodies as a stream and emit each received stream item as a separate message.",
          "type": "boolean",
          "default": false
        },
        "inline_response_fast_path": {
          "description": "(Consumer only) If true, compatible `http -> response` routes may bypass the normal route consumer/worker/disposition pipeline\nand reply inline for lower latency. Defaults to true. Set to false to force the normal route path.",
          "type": [
            "boolean",
            "null"
          ],
          "default": true
        },
        "server_protocol": {
          "description": "(Consumer only) Restrict which HTTP protocol versions a server listener accepts.\nDefaults to `auto` (HTTP/1.1 + HTTP/2). On cleartext listeners, `http2_only`\nmeans HTTP/2 prior-knowledge (h2c) only.",
          "$ref": "#/$defs/HttpServerProtocol",
          "default": "auto"
        },
        "stream_response_to": {
          "description": "(Publisher only) Optional endpoint that receives streamed HTTP response items as correlated messages.\n\nUse a `stream_buffer` endpoint here when callers need to read streamed\nresponse items later through a normal mq-bridge consumer. Each streamed\nitem is published with `correlation_id`, `http_stream_id`,\n`http_stream_index`, `http_stream_format`, and `http_stream_end`\nmetadata. If the request message has no `correlation_id`, the HTTP\npublisher uses `format!(\"{:032x}\", request.message_id)` so callers can\nderive the consumer correlation id before calling `send`.",
          "anyOf": [
            {
              "$ref": "#/$defs/Endpoint"
            },
            {
              "type": "null"
            }
          ]
        },
        "batch_concurrency": {
          "description": "(Publisher only) The number of concurrent HTTP requests to send in a batch. Defaults to 20.",
          "type": [
            "integer",
            "null"
          ],
          "format": "uint",
          "minimum": 0
        },
        "tcp_keepalive_ms": {
          "description": "(Publisher only) TCP keepalive timeout for the underlying connection pool in milliseconds. Defaults to 60000ms.",
          "type": [
            "integer",
            "null"
          ],
          "format": "uint64",
          "minimum": 0
        },
        "pool_idle_timeout_ms": {
          "description": "(Publisher only) Timeout for idle connections in the connection pool in milliseconds. Defaults to 90000ms.",
          "type": [
            "integer",
            "null"
          ],
          "format": "uint64",
          "minimum": 0
        },
        "compression": {
          "description": "(Publisher only) Codec for the request body (`none`, `gzip`, `lz4`, `zstd`); overrides\n`compression_enabled`. `lz4` is non-standard (mq-bridge peers only). Ignored on a consumer —\nenable response compression with `compression_enabled`. Defaults to `none`.",
          "$ref": "#/$defs/Compression",
          "default": "none"
        },
        "compression_enabled": {
          "description": "Turns compression on. Publisher: compress the request body with gzip (unless `compression`\nsets another codec). Consumer: compress responses, negotiating the best codec the client's\n`Accept-Encoding` accepts. Defaults to off.",
          "type": [
            "boolean",
            "null"
          ]
        },
        "compression_threshold_bytes": {
          "description": "Minimum message size in bytes to compress. Messages smaller than this are sent uncompressed. Defaults to 1024 bytes.",
          "type": [
            "integer",
            "null"
          ],
          "format": "uint",
          "minimum": 0,
          "default": null
        },
        "concurrency_limit": {
          "description": "(Consumer only) Maximum number of concurrent requests to handle. Defaults to 100.",
          "type": [
            "integer",
            "null"
          ],
          "format": "uint",
          "minimum": 0
        },
        "basic_auth": {
          "description": "HTTP Basic Authentication credentials (username, password). For consumers: validates incoming requests. For publishers: adds Authorization header.",
          "type": [
            "array",
            "null"
          ],
          "format": "password",
          "prefixItems": [
            {
              "type": "string"
            },
            {
              "type": "string"
            }
          ],
          "minItems": 2,
          "maxItems": 2
        },
        "custom_headers": {
          "description": "Custom headers as key-value pairs (e.g., {\"X-API-Key\": \"token123\"}). Added to outgoing HTTP headers for both consumers and publishers.",
          "type": "object",
          "format": "password",
          "additionalProperties": {
            "type": "string"
          }
        },
        "shared": {
          "description": "(Publisher only) Share one HTTP client per connection (default: true); false forces a dedicated client.",
          "type": [
            "boolean",
            "null"
          ],
          "default": true
        }
      },
      "additionalProperties": false,
      "required": [
        "url"
      ]
    },
    "HttpServerProtocol": {
      "description": "Supported inbound HTTP protocols for server listeners.",
      "oneOf": [
        {
          "description": "Accept both HTTP/1.1 and HTTP/2, matching the current default behavior.",
          "type": "string",
          "const": "auto"
        },
        {
          "description": "Accept only HTTP/1.x connections.",
          "type": "string",
          "const": "http1_only"
        },
        {
          "description": "Accept only HTTP/2 connections.",
          "type": "string",
          "const": "http2_only"
        }
      ]
    },
    "WebSocketConfig": {
      "description": "WebSocket connection configuration.",
      "type": "object",
      "properties": {
        "url": {
          "description": "For consumers, the listen address (e.g. \"0.0.0.0:9000\"). For publishers, the target URL.",
          "type": "string",
          "format": "password"
        },
        "path": {
          "description": "(Consumer only) Optional request path filter. If set, only upgrade requests whose URI path matches exactly are delivered to this consumer.",
          "type": [
            "string",
            "null"
          ]
        },
        "message_id_header": {
          "description": "(Consumer only) Header key to extract the message ID from the WebSocket handshake. Defaults to \"message-id\".",
          "type": [
            "string",
            "null"
          ]
        },
        "routed_queue_capacity": {
          "description": "(Consumer only) Queue capacity for the routed adapter. Direct response routes do not use this queue. Defaults to 100.",
          "type": [
            "integer",
            "null"
          ],
          "format": "uint",
          "minimum": 0
        },
        "backlog": {
          "description": "(Consumer only) TCP listen backlog (pending-connection queue depth) for the accept socket.\nRaise this if high-concurrency handshake bursts are being dropped/reset before `accept()`\ncan keep up. Defaults to 4096, which is higher than the OS/tokio default of 1024.",
          "type": [
            "integer",
            "null"
          ],
          "format": "uint32",
          "minimum": 0
        },
        "execution_mode": {
          "description": "(Consumer only) Selects whether WebSocket routes run directly or through the routed pipeline.",
          "$ref": "#/$defs/WebSocketExecutionMode",
          "default": "auto"
        }
      },
      "additionalProperties": false,
      "required": [
        "url"
      ]
    },
    "WebSocketExecutionMode": {
      "description": "WebSocket route execution strategy.",
      "oneOf": [
        {
          "description": "Use direct per-connection handling for simple `websocket -> response` routes and fall back\nto the routed adapter with a warning when route semantics need the normal pipeline.",
          "type": "string",
          "const": "auto"
        },
        {
          "description": "Require direct per-connection handling. Startup fails if the route cannot run directly.",
          "type": "string",
          "const": "direct_only"
        },
        {
          "description": "Always use the normal routed consumer/worker/disposition pipeline.",
          "type": "string",
          "const": "routed"
        }
      ]
    },
    "IbmMqConfig": {
      "description": "Connection settings for the IBM MQ Queue Manager.",
      "type": "object",
      "properties": {
        "url": {
          "description": "Required. Connection URL in `host(port)` format. Supports comma-separated list for failover (e.g., `host1(1414),host2(1414)`). If it contains userinfo, it will be treated as a secret.",
          "type": "string",
          "format": "password"
        },
        "queue": {
          "description": "Target Queue name for point-to-point messaging. Optional if `topic` is set; defaults to route name if omitted.",
          "type": [
            "string",
            "null"
          ]
        },
        "topic": {
          "description": "Target Topic string for Publish/Subscribe. If set, enables **Subscriber mode** (Consumer) or publishes to a topic (Publisher). Optional if `queue` is set.",
          "type": [
            "string",
            "null"
          ]
        },
        "queue_manager": {
          "description": "Required. Name of the Queue Manager to connect to (e.g., `QM1`).",
          "type": "string"
        },
        "channel": {
          "description": "Required. Server Connection (SVRCONN) Channel name defined on the QM.",
          "type": "string"
        },
        "username": {
          "description": "Username for authentication. Optional; required if the channel enforces authentication",
          "type": [
            "string",
            "null"
          ]
        },
        "password": {
          "description": "Password for authentication. Optional; required if the channel enforces authentication.",
          "type": [
            "string",
            "null"
          ],
          "format": "password"
        },
        "tls": {
          "description": "TLS configuration settings (e.g., keystore paths). Optional.",
          "$ref": "#/$defs/IbmTlsConfig",
          "default": {
            "required": false,
            "cipher_spec": null,
            "cert_file": null,
            "cert_password": null,
            "accept_invalid_certs": false
          }
        },
        "max_message_size": {
          "description": "Maximum message size in bytes (default: 4MB). Optional.",
          "type": "integer",
          "format": "uint",
          "minimum": 0,
          "default": 4194304
        },
        "wait_timeout_ms": {
          "description": "(Consumer only) Polling timeout in milliseconds (default: 1000ms). Optional.",
          "type": "integer",
          "format": "int32",
          "default": 1000
        },
        "internal_buffer_size": {
          "description": "Internal buffer size for the channel. Defaults to 100.",
          "type": [
            "integer",
            "null"
          ],
          "format": "uint",
          "minimum": 0,
          "default": null
        },
        "disable_status_inq": {
          "description": "If false, attempt to open the queue with INQUIRE permissions to fetch queue depth for status checks. Defaults to false.",
          "type": "boolean",
          "default": false
        }
      },
      "additionalProperties": false,
      "required": [
        "url",
        "queue_manager",
        "channel"
      ]
    },
    "IbmTlsConfig": {
      "description": "TLS configuration for the IBM MQ native client.\n\nThe IBM MQ client doesn't consume PEM files, so this uses MQ-native field\nnames rather than the generic [`TlsConfig`] used by the other endpoints.",
      "type": "object",
      "properties": {
        "required": {
          "description": "If true, enable TLS/SSL.",
          "type": "boolean",
          "default": false
        },
        "cipher_spec": {
          "description": "TLS CipherSpec (e.g., `ANY_TLS12`). Required for encrypted connections. IBM MQ-specific.",
          "type": [
            "string",
            "null"
          ]
        },
        "cert_file": {
          "description": "For IBM MQ this is the CMS key repository stem (e.g. `/path/to/tls` for `tls.kdb`/`tls.sth`),\nnot a PEM file. Exposed as `cert_file` for config parity with the generic `TlsConfig`;\nthe MQ-native name `key_repository` is still accepted.",
          "type": [
            "string",
            "null"
          ]
        },
        "cert_password": {
          "description": "Password unlocking the key repository. Requires an IBM MQ client/server at 9.3.0.0+.\nExposed as `cert_password` for parity with `TlsConfig`; alias `key_repository_password`.",
          "type": [
            "string",
            "null"
          ],
          "format": "password"
        },
        "accept_invalid_certs": {
          "description": "If true, disable server certificate verification (insecure).",
          "type": "boolean",
          "default": false
        },
        "key_repository": {
          "description": "MQ-native alias for `cert_file`: the CMS key repository stem (e.g. `/path/to/tls` for `tls.kdb`/`tls.sth`).",
          "type": [
            "string",
            "null"
          ]
        },
        "key_repository_password": {
          "description": "MQ-native alias for `cert_password`: password unlocking the key repository. Requires an IBM MQ client/server at 9.3.0.0+.",
          "type": [
            "string",
            "null"
          ],
          "format": "password"
        }
      },
      "additionalProperties": false
    },
    "ZeroMqConfig": {
      "type": "object",
      "properties": {
        "url": {
          "description": "The ZeroMQ URL (e.g., \"tcp://127.0.0.1:5555\").",
          "type": "string",
          "format": "password"
        },
        "socket_type": {
          "description": "The socket type (PUSH, PULL, PUB, SUB, REQ, REP).",
          "anyOf": [
            {
              "$ref": "#/$defs/ZeroMqSocketType"
            },
            {
              "type": "null"
            }
          ],
          "default": null
        },
        "topic": {
          "description": "(Consumer only) The ZeroMQ topic (for SUB sockets).",
          "type": [
            "string",
            "null"
          ]
        },
        "bind": {
          "description": "If true, bind to the address. If false, connect.",
          "type": "boolean",
          "default": false
        },
        "internal_buffer_size": {
          "description": "Internal buffer size for the channel. Defaults to 128. `zmq` backend only — `omq`\napplies HWM backpressure on the socket itself and ignores this.",
          "type": [
            "integer",
            "null"
          ],
          "format": "uint",
          "minimum": 0,
          "default": null
        },
        "format": {
          "description": "Wire format: `json` wraps the CanonicalMessage; `raw` sends payload bytes per frame; `raw_framed` adds a JSON metadata frame. Default `raw_framed`.\nREQ/REP replies are the exception: a REP peer always answers with a JSON array of\ncanonical messages and a REQ publisher always decodes one, whatever `format` is set to.",
          "$ref": "#/$defs/ZeroMqFormat",
          "default": "raw_framed"
        },
        "backend": {
          "description": "Backend: `try_omq` (default, prefer `omq` and fall back to `zmq`), `zmq` (the `zeromq` crate) or `omq` (the `omq-tokio` backend). `omq` needs the `zeromq-omq` build feature.",
          "$ref": "#/$defs/ZeroMqBackend",
          "default": "try_omq"
        },
        "request_timeout_ms": {
          "description": "(REQ publisher only) Timeout in ms for one request/reply exchange before it is reported as failed. Defaults to 30000.",
          "type": [
            "integer",
            "null"
          ],
          "format": "uint64",
          "minimum": 0,
          "default": null
        }
      },
      "additionalProperties": false,
      "required": [
        "url"
      ]
    },
    "ZeroMqSocketType": {
      "description": "ZeroMQ socket type.\n\nDefines the messaging pattern for ZeroMQ connections.\nDifferent patterns support different communication paradigms (request-reply, publish-subscribe, etc.).",
      "type": "string",
      "enum": [
        "push",
        "pull",
        "pub",
        "sub",
        "req",
        "rep"
      ]
    },
    "ZeroMqFormat": {
      "description": "ZeroMQ wire format.\n\n`json` wraps each message as a JSON CanonicalMessage (batched into one frame);\n`raw` sends/receives the payload bytes directly, one frame per message (metadata\nis not transmitted); `raw_framed` (default) sends a two-frame message — a JSON metadata\nframe followed by the raw payload frame — keeping the payload binary-safe while still\ncarrying headers. Use `raw`/`raw_framed` for binary feeds such as JPEG, Avro or Protobuf.",
      "type": "string",
      "enum": [
        "json",
        "raw",
        "raw_framed"
      ]
    },
    "ZeroMqBackend": {
      "description": "ZeroMQ backend implementation.\n\n`omq` uses `omq-tokio` (omq.rs) — much faster on the per-message `raw`/`raw_framed`\npath and adds CURVE/PLAIN security — and requires the `zeromq-omq` build feature\n(MSRV 1.93). `zmq` uses the `zeromq` crate (pure-Rust zmq.rs) and covers every\nsocket type.\n\n`try_omq` is the default: it picks `omq` when that feature is compiled in and the\nconfigured socket type is supported there, and falls back to `zmq` otherwise, so a\nbuild without `zeromq-omq` still runs. Name a backend explicitly to make the choice\na hard requirement instead — a missing feature is then a startup error.",
      "oneOf": [
        {
          "type": "string",
          "enum": [
            "zmq",
            "omq"
          ]
        },
        {
          "description": "Prefer `omq`, fall back to `zmq` when it is unavailable for this build or socket.",
          "type": "string",
          "const": "try_omq"
        }
      ]
    },
    "RedisStreamsConfig": {
      "description": "Configuration for a Redis Streams endpoint.\n\nPublishers `XADD` to the stream; consumers read via a consumer group\n(`XREADGROUP` + `XACK`) by default, or ephemerally via `XREAD` from new\nmessages when `subscriber_mode` is set.",
      "type": "object",
      "properties": {
        "url": {
          "description": "Redis URL, `redis://` or `rediss://` for TLS. Userinfo is treated as a secret.",
          "type": "string",
          "format": "password"
        },
        "stream": {
          "description": "The stream key to publish to or read from. Defaults to the route name.",
          "type": [
            "string",
            "null"
          ]
        },
        "group": {
          "description": "(Consumer) Group name. Defaults to `{APP_NAME}-{stream}`; ignored in `subscriber_mode`.",
          "type": [
            "string",
            "null"
          ]
        },
        "consumer_name": {
          "description": "(Consumer) Consumer name within the group. Defaults to a unique per-instance id.",
          "type": [
            "string",
            "null"
          ]
        },
        "subscriber_mode": {
          "description": "(Consumer) Read ephemerally via `XREAD` from new messages (no group/acks). Default false.",
          "type": "boolean",
          "default": false
        },
        "block_ms": {
          "description": "(Consumer) Block timeout in milliseconds for each read. Defaults to 5000ms.",
          "type": [
            "integer",
            "null"
          ],
          "format": "uint64",
          "minimum": 0
        },
        "read_from_start": {
          "description": "(Consumer) On group creation, start from the stream beginning (\"0\") not \"$\". Default false.",
          "type": "boolean",
          "default": false
        },
        "redelivery_timeout_ms": {
          "description": "(Consumer) Redeliver entries pending ≥ this long via `XAUTOCLAIM`; 0 disables. Default 60000ms.",
          "type": [
            "integer",
            "null"
          ],
          "format": "uint64",
          "minimum": 0
        },
        "maxlen": {
          "description": "(Publisher) If set, cap the stream length with `XADD MAXLEN`.",
          "type": [
            "integer",
            "null"
          ],
          "format": "uint",
          "minimum": 0
        },
        "approx_trim": {
          "description": "(Publisher) Use approximate (`~`) trimming when `maxlen` is set. Defaults to true.",
          "type": [
            "boolean",
            "null"
          ]
        },
        "username": {
          "description": "Optional username for authentication (Redis ACL).",
          "type": [
            "string",
            "null"
          ]
        },
        "password": {
          "description": "Optional password for authentication.",
          "type": [
            "string",
            "null"
          ],
          "format": "password"
        },
        "internal_buffer_size": {
          "description": "Internal buffer size for the consumer channel. Defaults to 128.",
          "type": [
            "integer",
            "null"
          ],
          "format": "uint",
          "minimum": 0
        },
        "reader_connections": {
          "description": "(Consumer) Parallel `XREADGROUP` reader connections fanned out across the group. Default 1.\nIgnored in `subscriber_mode`.",
          "type": [
            "integer",
            "null"
          ],
          "format": "uint",
          "minimum": 0
        }
      },
      "additionalProperties": false,
      "required": [
        "url"
      ]
    },
    "GrpcConfig": {
      "type": "object",
      "properties": {
        "url": {
          "description": "The gRPC server URL (e.g., \"http://localhost:50051\" for client or \"0.0.0.0:50051\" for server mode).",
          "type": "string",
          "format": "password"
        },
        "topic": {
          "description": "Topic / subject used for both subscribe and publish paths.",
          "type": [
            "string",
            "null"
          ]
        },
        "consumer_id": {
          "description": "Stable subscription identity used for ACK tracking and redelivery. Defaults to a\nfresh id per consumer; set it to be redelivered unacknowledged messages on reconnect.",
          "type": [
            "string",
            "null"
          ],
          "default": null
        },
        "timeout_ms": {
          "description": "Timeout in milliseconds.\n- Client mode: used as the connection timeout and per-request deadline.\n- Server mode: applied as the per-request deadline on the embedded server.",
          "type": [
            "integer",
            "null"
          ],
          "format": "uint64",
          "minimum": 0
        },
        "tls": {
          "description": "TLS configuration.",
          "$ref": "#/$defs/TlsConfig",
          "default": {
            "required": false,
            "ca_file": null,
            "cert_file": null,
            "key_file": null,
            "cert_password": null,
            "accept_invalid_certs": false
          }
        },
        "server_mode": {
          "description": "If `true`, start an embedded tonic gRPC server that accepts incoming `Publish` /\n`PublishBatch` RPCs. If `false` (the default), connect to a remote server as a client.",
          "type": "boolean",
          "default": false
        },
        "initial_stream_window_size": {
          "description": "HTTP/2 stream-level initial window size in bytes. Applies in both modes.",
          "type": [
            "integer",
            "null"
          ],
          "format": "uint32",
          "minimum": 0,
          "default": null
        },
        "initial_connection_window_size": {
          "description": "HTTP/2 connection-level initial window size in bytes. Applies in both modes.",
          "type": [
            "integer",
            "null"
          ],
          "format": "uint32",
          "minimum": 0,
          "default": null
        },
        "concurrency_limit_per_connection": {
          "description": "Maximum number of concurrent requests handled per connection. **Server-mode only.**",
          "type": [
            "integer",
            "null"
          ],
          "format": "uint",
          "minimum": 0,
          "default": null
        },
        "http2_keepalive_interval_ms": {
          "description": "HTTP/2 keepalive ping interval in milliseconds. Applies in both modes. Default disabled",
          "type": [
            "integer",
            "null"
          ],
          "format": "uint64",
          "minimum": 0,
          "default": null
        },
        "http2_keepalive_timeout_ms": {
          "description": "Timeout for a keepalive ping acknowledgement in milliseconds. Applies in both modes.",
          "type": [
            "integer",
            "null"
          ],
          "format": "uint64",
          "minimum": 0,
          "default": null
        },
        "max_decoding_message_size": {
          "description": "Maximum size of a decoded incoming message in bytes. Applies in both modes. Default 4 MiB.",
          "type": [
            "integer",
            "null"
          ],
          "format": "uint",
          "minimum": 0,
          "default": null
        },
        "max_encoding_message_size": {
          "description": "Maximum size of an encoded outgoing message in bytes. Default unlimited.",
          "type": [
            "integer",
            "null"
          ],
          "format": "uint",
          "minimum": 0,
          "default": null
        },
        "descriptor_set_path": {
          "description": "Compiled protobuf FileDescriptorSet for dynamic client mode.",
          "type": [
            "string",
            "null"
          ],
          "default": null
        },
        "service_name": {
          "description": "Fully-qualified protobuf service name for dynamic client mode.",
          "type": [
            "string",
            "null"
          ],
          "default": null
        },
        "method_name": {
          "description": "RPC method name for dynamic client mode.",
          "type": [
            "string",
            "null"
          ],
          "default": null
        },
        "request": {
          "description": "JSON request mapped to the dynamic protobuf input message.",
          "default": null
        },
        "server_streaming": {
          "description": "Use a server-streaming dynamic RPC. False selects unary.",
          "type": "boolean",
          "default": false
        },
        "shared": {
          "description": "(Publisher only) Share one gRPC channel per connection (default: true); false forces a dedicated channel.",
          "type": [
            "boolean",
            "null"
          ],
          "default": true
        }
      },
      "additionalProperties": false,
      "required": [
        "url"
      ]
    },
    "SqlxConfig": {
      "description": "General SQLx connection configuration.",
      "type": "object",
      "properties": {
        "url": {
          "description": "Database connection URL. If it contains userinfo, it will be treated as a secret.",
          "type": "string",
          "format": "password"
        },
        "username": {
          "description": "Optional username. Takes precedence over any credentials embedded in the `url`.",
          "type": [
            "string",
            "null"
          ],
          "default": null
        },
        "password": {
          "description": "Optional password. Takes precedence over any credentials embedded in the `url`.",
          "type": [
            "string",
            "null"
          ],
          "format": "password",
          "default": null
        },
        "table": {
          "description": "The table to interact with.",
          "type": "string"
        },
        "insert_query": {
          "description": "(Publisher only) Optional. A custom SQL INSERT query. Use `?` as a placeholder for the payload.\nIf not provided, a default `INSERT INTO {table} (payload) VALUES (?)` is used.\n\nFor multi-column inserts, embed explicit source tokens directly in the query:\n`${metadata:<key>}` binds `message.metadata[\"<key>\"]`, and `${payload:<field>}`\nbinds the top-level JSON field `<field>` of the payload (types preserved:\nnumbers/bools stay numeric/bool). There is no fallback between the two: an\nabsent metadata key, non-JSON payload, or missing/non-scalar field binds SQL NULL.\nExample: `INSERT INTO orders (customer_id, sku, qty) VALUES (${metadata:customer_id}, ${payload:sku}, ${payload:qty})`.\nA query with no `${...}` tokens behaves exactly as before (whole payload bound once).\n`auto_create_table` is not supported together with a token-based query.\n\nTokens bind as text/number/bool; Postgres won't implicitly cast text into a\n`numeric`/`timestamptz` column (these arrive as JSON strings from a sql source).\nAdd an explicit cast next to the token — it is preserved verbatim in the SQL:\n`VALUES (${payload:amount}::numeric, ${payload:created_at}::timestamptz)`.",
          "type": [
            "string",
            "null"
          ]
        },
        "select_query": {
          "description": "(Consumer only) Optional. A custom SQL SELECT query to fetch messages. This is only supported for PostgreSQL and Microsoft SQL Server.\nThe query must include a placeholder for the batch size (`$1` for PostgreSQL, `@p1` for SQL Server).\nThe bridge will bind the route's `batch_size` to this placeholder.",
          "type": [
            "string",
            "null"
          ]
        },
        "delete_after_read": {
          "description": "(Consumer only) If true, delete messages after processing.",
          "type": "boolean",
          "default": false
        },
        "cursor_column": {
          "description": "(Consumer only) Read an existing table **non-destructively** and resumably, paging by this\nmonotonic column (`SELECT * FROM {table} WHERE {cursor_column} > $last ORDER BY {cursor_column} ASC LIMIT n`)\nand persisting the last read value under `cursor_id`. Does not delete/lock source rows.\nMutually exclusive with `delete_after_read`.",
          "type": [
            "string",
            "null"
          ]
        },
        "cursor_id": {
          "description": "(Consumer only) Cursor id used to key the persisted resume position. Recommended when\n`cursor_column` is set: without it, progress is not persisted and every restart re-copies\nfrom the beginning.",
          "type": [
            "string",
            "null"
          ]
        },
        "checkpoint_store": {
          "description": "(Consumer only) Where to persist the resume cursor in `cursor_column` mode. A URL selects the\nbackend; a bare name (or `/name`) reuses the **source** datastore with that table name:\n- absent → source datastore, table `mqb_cursors_<source_table>` (auto-unique)\n- `/my_cursors` → source datastore, table `my_cursors`\n- `file:///var/lib/mqb/cursors.json` → local JSON file (read-only / write-restricted sources)\n- `postgres://user@host/db/table` or `mysql://host/db/table` → external SQL table (table optional)\n- `mongodb://host/db/collection` → external MongoDB collection (collection optional)\n- `s3://bucket/prefix` (also `gs://`, `az://`, `abfs://`) → cloud object store; creds via env\n\nWhen no table/collection is named, it defaults to `mqb_cursors_<source_table>`.\nMay embed connection credentials, so it is treated as a secret.",
          "type": [
            "string",
            "null"
          ],
          "format": "password"
        },
        "auto_create_table": {
          "description": "(Publisher only) If true, automatically create the table and indexes if they don't exist. Defaults to false.",
          "type": "boolean",
          "default": false
        },
        "bulk_copy": {
          "description": "(Publisher only) PostgreSQL only. Bulk-load batches via `COPY FROM STDIN` (much faster than multi-row INSERT). Requires a token-based `insert_query`; no `ON CONFLICT`/`RETURNING`.",
          "type": "boolean",
          "default": false
        },
        "polling_interval_ms": {
          "description": "(Consumer only) Polling interval in milliseconds. Defaults to 100ms.",
          "type": [
            "integer",
            "null"
          ],
          "format": "uint64",
          "minimum": 0
        },
        "max_polling_interval_ms": {
          "description": "(Consumer only) If set, the poll interval backs off exponentially from `polling_interval_ms`\nup to this value while drained, resetting on new rows. Unset = constant interval.",
          "type": [
            "integer",
            "null"
          ],
          "format": "uint64",
          "minimum": 0
        },
        "publication": {
          "description": "(Consumer only, PostgreSQL) If set, consume via logical-replication CDC instead of cursor\npolling: streams inserts/updates/deletes from this publication. Requires the `postgres-cdc`\nfeature and a Postgres URL. For full control use the dedicated `postgres_cdc` endpoint.",
          "type": [
            "string",
            "null"
          ]
        },
        "slot_name": {
          "description": "(Consumer only, CDC) Replication slot name; created if missing. Defaults to `mq_bridge_slot`.",
          "type": [
            "string",
            "null"
          ]
        },
        "create_publication": {
          "description": "(Consumer only, CDC) When `publication` is set, create it if missing (default false).\nNeeds table-owner privilege: it is auto-published `FOR TABLE {table}`.",
          "type": "boolean",
          "default": false
        },
        "source_metadata": {
          "description": "(Consumer only) Include authoritative `mqb.src.sqlx_*` source positions; `cursor_column` must then be a unique integer. Defaults to false.",
          "type": "boolean",
          "default": false
        },
        "tls": {
          "description": "TLS configuration for the database connection.",
          "$ref": "#/$defs/TlsConfig",
          "default": {
            "required": false,
            "ca_file": null,
            "cert_file": null,
            "key_file": null,
            "cert_password": null,
            "accept_invalid_certs": false
          }
        },
        "max_connections": {
          "description": "Maximum number of connections in the pool. Defaults to 10.",
          "type": [
            "integer",
            "null"
          ],
          "format": "uint32",
          "minimum": 0
        },
        "min_connections": {
          "description": "Minimum number of connections to keep in the pool. Defaults to 0.",
          "type": [
            "integer",
            "null"
          ],
          "format": "uint32",
          "minimum": 0
        },
        "acquire_timeout_ms": {
          "description": "Timeout for acquiring a connection from the pool in milliseconds. Defaults to 30000ms.",
          "type": [
            "integer",
            "null"
          ],
          "format": "uint64",
          "minimum": 0
        },
        "idle_timeout_ms": {
          "description": "Maximum idle time for a connection in milliseconds. Defaults to 600000ms (10 minutes).",
          "type": [
            "integer",
            "null"
          ],
          "format": "uint64",
          "minimum": 0
        },
        "max_lifetime_ms": {
          "description": "Maximum lifetime of a connection in milliseconds. Defaults to 1800000ms (30 minutes).",
          "type": [
            "integer",
            "null"
          ],
          "format": "uint64",
          "minimum": 0
        },
        "test_before_acquire": {
          "description": "Ping each pooled connection before handing it out (default false). Costs a round-trip per acquire.",
          "type": [
            "boolean",
            "null"
          ]
        },
        "shared": {
          "description": "Share one connection pool per connection (default: true); false forces a dedicated pool.",
          "type": [
            "boolean",
            "null"
          ],
          "default": true
        }
      },
      "additionalProperties": false,
      "required": [
        "url",
        "table"
      ]
    },
    "ClickHouseConfig": {
      "description": "ClickHouse endpoint configuration (talks the ClickHouse HTTP interface).\n\nAs a **publisher** it batch-inserts messages using `FORMAT JSONEachRow` — by default the whole\nmessage payload (which must be a JSON object) becomes one row; set `columns` to build each row\nfrom explicit `${payload:<field>}` / `${metadata:<key>}` tokens instead. As a **consumer** it\nreads an existing table **non-destructively** by paging over a monotonic `cursor_column`\n(ClickHouse has no native queue/pub-sub), serializing each row to a JSON payload.",
      "type": "object",
      "properties": {
        "url": {
          "description": "ClickHouse HTTP endpoint URL, e.g. `http://localhost:8123` (or `https://…`). If it contains\nuserinfo, it will be treated as a secret.",
          "type": "string",
          "format": "password"
        },
        "username": {
          "description": "Optional username. Takes precedence over any credentials embedded in the `url`. Defaults to `default`.",
          "type": [
            "string",
            "null"
          ],
          "default": null
        },
        "password": {
          "description": "Optional password. Takes precedence over any credentials embedded in the `url`.",
          "type": [
            "string",
            "null"
          ],
          "format": "password",
          "default": null
        },
        "database": {
          "description": "Database name. Defaults to `default`.",
          "type": [
            "string",
            "null"
          ]
        },
        "table": {
          "description": "The table to read from / write to. May be schema-qualified (`db.table`).",
          "type": "string"
        },
        "columns": {
          "description": "(Publisher only) Optional per-column mapping. Each entry maps a target column name to a value\ntoken: `${payload:<field>}` takes the top-level JSON field `<field>` of the payload (JSON type\npreserved), `${metadata:<key>}` takes `message.metadata[\"<key>\"]` (as a string), and any other\nvalue is inserted literally. When omitted, the whole payload JSON object is inserted as one row.",
          "type": [
            "object",
            "null"
          ],
          "additionalProperties": {
            "type": "string"
          }
        },
        "async_insert": {
          "description": "(Publisher only) If true, set the ClickHouse `async_insert=1` server setting so inserts are\nbuffered server-side. Defaults to false.",
          "type": "boolean",
          "default": false
        },
        "wait_for_async_insert": {
          "description": "(Publisher only) With `async_insert`, wait for the server to flush before acking. Defaults to\ntrue (durable). False = fire-and-forget: faster, but a crash before flush can drop the batch.",
          "type": [
            "boolean",
            "null"
          ],
          "default": null
        },
        "cursor_column": {
          "description": "(Consumer only) Read an existing table **non-destructively** and resumably, paging by this\nmonotonic column (`SELECT … WHERE {cursor_column} > {last} ORDER BY {cursor_column} ASC LIMIT n`)\nand persisting the last read value under `cursor_id`.",
          "type": [
            "string",
            "null"
          ]
        },
        "cursor_id": {
          "description": "(Consumer only) Cursor id used to key the persisted resume position. Without it, progress is not\npersisted and every restart re-copies from the beginning.",
          "type": [
            "string",
            "null"
          ]
        },
        "checkpoint_store": {
          "description": "(Consumer only) Where to persist the resume cursor. Because ClickHouse is unsuited to per-row\ncursor upserts, a durable checkpoint requires an **external** store URL:\n- `file:///var/lib/mqb/cursors.json` → local JSON file\n- `postgres://user@host/db/table` / `mysql://host/db/table` → external SQL table (table optional)\n- `mongodb://host/db/collection` → external MongoDB collection (collection optional)\n- `s3://bucket/prefix` (also `gs://`, `az://`, `abfs://`) → cloud object store; creds via env\n\nMay embed connection credentials, so it is treated as a secret.",
          "type": [
            "string",
            "null"
          ],
          "format": "password"
        },
        "select_columns": {
          "description": "(Consumer only) Columns to select in `cursor_column` mode. Defaults to `*`.",
          "type": [
            "string",
            "null"
          ]
        },
        "polling_interval_ms": {
          "description": "(Consumer only) Polling interval in milliseconds when the table is drained. Defaults to 100ms.",
          "type": [
            "integer",
            "null"
          ],
          "format": "uint64",
          "minimum": 0
        },
        "max_polling_interval_ms": {
          "description": "(Consumer only) If set, the poll interval backs off exponentially from `polling_interval_ms`\nup to this value while drained, resetting on new rows. Unset = constant interval.",
          "type": [
            "integer",
            "null"
          ],
          "format": "uint64",
          "minimum": 0
        },
        "request_timeout_ms": {
          "description": "Request timeout in milliseconds for ClickHouse HTTP calls (inserts, cursor reads, status).\nUnset = no timeout (wait indefinitely), which suits very large batch inserts.",
          "type": [
            "integer",
            "null"
          ],
          "format": "uint64",
          "minimum": 0
        },
        "connect_timeout_ms": {
          "description": "Connection (TCP + TLS handshake) timeout in milliseconds. Defaults to 10000ms.",
          "type": [
            "integer",
            "null"
          ],
          "format": "uint64",
          "minimum": 0
        },
        "tls": {
          "description": "TLS configuration for `https://` connections.",
          "$ref": "#/$defs/TlsConfig",
          "default": {
            "required": false,
            "ca_file": null,
            "cert_file": null,
            "key_file": null,
            "cert_password": null,
            "accept_invalid_certs": false
          }
        },
        "compression": {
          "description": "HTTP body compression for inserts and cursor reads (`none`, `gzip`, `lz4`, `zstd`). Applied\nas `Content-Encoding` on the request body and negotiated on the response via `Accept-Encoding`.\n`lz4`/`zstd` are faster than `gzip`; all are understood natively by ClickHouse. Defaults to `gzip`.",
          "$ref": "#/$defs/Compression",
          "default": "gzip"
        }
      },
      "additionalProperties": false,
      "required": [
        "url",
        "table"
      ]
    },
    "PostgresCdcConfig": {
      "description": "Postgres logical-replication CDC source (pgoutput). Source-only.",
      "type": "object",
      "properties": {
        "url": {
          "description": "Connection URL, e.g. `postgres://user:pass@host:5432/dbname`.",
          "type": "string",
          "format": "password"
        },
        "publication": {
          "description": "Publication name (must already exist; defines which tables are captured).",
          "type": "string"
        },
        "source_metadata": {
          "description": "Include authoritative `mqb.src.postgres_*` source positions. Defaults to false.",
          "type": "boolean",
          "default": false
        },
        "slot_name": {
          "description": "Replication slot name; created if missing when `create_slot` is true.",
          "type": "string",
          "default": "mq_bridge_slot"
        },
        "create_slot": {
          "description": "Create the replication slot if it does not exist.",
          "type": "boolean",
          "default": true
        },
        "create_publication": {
          "description": "Create the `publication` if missing (default false; leave off if it pre-exists).\nNeeds table ownership for `publication_tables`, or superuser when none are set (`FOR ALL TABLES`).",
          "type": "boolean",
          "default": false
        },
        "publication_tables": {
          "description": "Tables to include when managing the publication (`create_publication`); may be `schema.table`.\nMissing ones are added to an existing publication (never removed). Empty = `FOR ALL TABLES` (needs superuser).",
          "type": "array",
          "items": {
            "type": "string"
          },
          "default": []
        },
        "temporary_slot": {
          "description": "Ephemeral run: drop the slot when the route stops. Not restart-safe; a hard crash leaks it.",
          "type": "boolean",
          "default": false
        },
        "cursor_id": {
          "description": "Checkpoint key for persisting the confirmed LSN across restarts (optional; the slot is authoritative).",
          "type": [
            "string",
            "null"
          ]
        },
        "checkpoint_store": {
          "description": "Checkpoint store spec (e.g. `file:///path`, `s3://bucket/prefix`); defaults to the source database.",
          "type": [
            "string",
            "null"
          ],
          "format": "password"
        },
        "status_interval_ms": {
          "description": "Standby-status-update interval in ms; must be shorter than the server's `wal_sender_timeout`.",
          "type": "integer",
          "format": "uint64",
          "minimum": 0,
          "default": 10000
        },
        "tls": {
          "description": "TLS configuration for the replication connection.",
          "$ref": "#/$defs/TlsConfig",
          "default": {
            "required": false,
            "ca_file": null,
            "cert_file": null,
            "key_file": null,
            "cert_password": null,
            "accept_invalid_certs": false
          }
        }
      },
      "additionalProperties": false,
      "required": [
        "url",
        "publication"
      ]
    },
    "StreamBufferConfig": {
      "description": "Configuration for the correlated in-process stream response buffer.",
      "type": "object",
      "properties": {
        "topic": {
          "description": "Shared buffer topic used by both the publisher and correlated consumers.",
          "type": "string"
        },
        "correlation_id": {
          "description": "Consumer-only correlation id partition to read from.\n\nLeave this unset for the publisher endpoint configured in\n`HttpConfig::stream_response_to`. Set it on consumers so a reader only\nreceives messages belonging to one request or response stream.",
          "type": [
            "string",
            "null"
          ]
        },
        "capacity": {
          "description": "Capacity of each correlation partition. Defaults to 100.",
          "type": [
            "integer",
            "null"
          ],
          "format": "uint",
          "minimum": 0
        }
      },
      "additionalProperties": false,
      "required": [
        "topic"
      ]
    },
    "SwitchConfig": {
      "type": "object",
      "properties": {
        "metadata_key": {
          "description": "The metadata key to inspect for routing decisions.",
          "type": "string"
        },
        "cases": {
          "description": "A map of values to endpoints.",
          "type": "object",
          "additionalProperties": {
            "$ref": "#/$defs/Endpoint"
          }
        },
        "default": {
          "description": "The default endpoint if no case matches.",
          "anyOf": [
            {
              "$ref": "#/$defs/Endpoint"
            },
            {
              "type": "null"
            }
          ]
        }
      },
      "additionalProperties": false,
      "required": [
        "metadata_key",
        "cases"
      ]
    },
    "ResponseConfig": {
      "type": "object",
      "additionalProperties": false
    },
    "RequestForwardConfig": {
      "description": "Sends each message to a request-capable endpoint and forwards its response elsewhere.\n\nTurns a request/reply exchange (HTTP, or a request_reply NATS/Mongo/Memory endpoint) into\na one-way flow whose response lands on `forward_to` — e.g. IBM MQ → HTTP → IBM MQ. On\nrequest error/timeout the original message is forwarded instead (unchanged). Successful\nresponses carry the transport-native status (e.g. `http_status_code`), so a `switch` on\n`forward_to` can route them by status; a failed request forwards the original message with\nno status key, so catch failures on the switch's default branch.",
      "type": "object",
      "properties": {
        "to": {
          "description": "The request-capable endpoint to send each message to (e.g. an `http` client).",
          "$ref": "#/$defs/Endpoint"
        },
        "forward_to": {
          "description": "Where the response (or, on error, the original message) is forwarded.",
          "$ref": "#/$defs/Endpoint"
        }
      },
      "additionalProperties": false,
      "required": [
        "to",
        "forward_to"
      ]
    },
    "ConsumerConfig": {
      "description": "Fine-tuning options for a route's execution.\n\nThese options control concurrency, batching, and commit behavior for message processing.\n\n# Examples\n\n```\nuse mq_bridge::models::RouteOptions;\n\nlet options = RouteOptions {\n    description: \"My Route\".to_string(),\n    concurrency: 10,\n    batch_size: 5,\n    commit_concurrency_limit: 1024,\n    ..Default::default()\n};\n```",
      "type": "object",
      "properties": {
        "id": {
          "type": "string",
          "default": "01a01af1-8c9f-7f93-9586-dad92d786e5a"
        },
        "name": {
          "type": "string",
          "default": ""
        },
        "endpoint": {
          "$ref": "#/$defs/Endpoint"
        },
        "comment": {
          "type": "string",
          "default": ""
        },
        "response": {
          "anyOf": [
            {
              "$ref": "#/$defs/ConsumerResponseConfig"
            },
            {
              "type": "null"
            }
          ]
        },
        "output": {
          "$ref": "#/$defs/ConsumerOutputConfig"
        },
        "message_capture": {
          "$ref": "#/$defs/ConsumerMessageCaptureConfig",
          "default": {
            "enabled": true,
            "keep_last": 100
          }
        },
        "description": {
          "description": "A human-readable description of the route's purpose. Defaults to an empty string.",
          "type": "string"
        },
        "concurrency": {
          "description": "(Optional) Number of concurrent processing tasks for this route. While it improves throughput for high-latency\nhandlers, it adds synchronization overhead for ordered commits and may lead to out-of-order processing\nin the handler. Above 1, whole batches may also reach the sink out of source order (rows keep their order\nwithin a batch) unless the sink declares itself order-sensitive, as `file` does.\nDefaults to 1.",
          "type": "integer",
          "format": "uint",
          "minimum": 1,
          "default": 1
        },
        "batch_size": {
          "description": "(Optional) Maximum number of messages to process in a single batch. The consumer waits for at least one message\nand then attempts to fetch more if available. Increasing this improves throughput but also increases\nthe potential impact of a single batch processing failure. Defaults to 512.",
          "type": "integer",
          "format": "uint",
          "minimum": 1,
          "default": 512
        },
        "commit_concurrency_limit": {
          "description": "(Optional) The maximum number of in-flight commit requests queued for ordered sequencing.\nLower values apply backpressure earlier; higher values allow larger commit backlogs.\nDefaults to 4096.",
          "type": "integer",
          "format": "uint",
          "minimum": 1,
          "default": 4096
        },
        "startup_timeout_ms": {
          "description": "Time to wait for a route to establish connections before startup fails. Defaults to 5000ms.",
          "type": "integer",
          "format": "uint64",
          "minimum": 0,
          "default": 5000
        },
        "reconnect_interval_ms": {
          "description": "Time to wait before reconnecting after a transient route failure. Defaults to 5000ms.",
          "type": "integer",
          "format": "uint64",
          "minimum": 0,
          "default": 5000
        },
        "empty_batch_delay_ms": {
          "description": "Delay after an empty receive batch to avoid hot polling. Set to 0 to only yield. Defaults to 10ms.",
          "type": "integer",
          "format": "uint64",
          "minimum": 0,
          "default": 10
        },
        "allow_fault_injection": {
          "description": "Allows fault-injection middleware such as random_panic. Disabled by default.",
          "type": "boolean"
        },
        "exit_on_empty": {
          "description": "If true, the route exits gracefully once the source yields an empty batch\n(drain-then-exit). Off by default — routes normally poll indefinitely.\nA drain that keeps failing to reconnect gives up and fails rather than retrying forever.",
          "type": "boolean"
        }
      },
      "required": [
        "endpoint"
      ]
    },
    "ConsumerResponseConfig": {
      "type": "object",
      "properties": {
        "headers": {
          "type": "object",
          "additionalProperties": {
            "type": "string"
          },
          "default": {}
        },
        "payload": {
          "type": "string",
          "default": ""
        }
      }
    },
    "ConsumerOutputConfig": {
      "oneOf": [
        {
          "type": "object",
          "properties": {
            "mode": {
              "type": "string",
              "const": "none"
            }
          },
          "required": [
            "mode"
          ]
        },
        {
          "type": "object",
          "properties": {
            "publisher": {
              "type": "string"
            },
            "publisher_id": {
              "type": [
                "string",
                "null"
              ]
            },
            "mode": {
              "type": "string",
              "const": "publisher"
            }
          },
          "required": [
            "mode",
            "publisher"
          ]
        },
        {
          "type": "object",
          "properties": {
            "response": {
              "anyOf": [
                {
                  "$ref": "#/$defs/ConsumerResponseConfig"
                },
                {
                  "type": "null"
                }
              ]
            },
            "mode": {
              "type": "string",
              "const": "response"
            }
          },
          "required": [
            "mode"
          ]
        }
      ]
    },
    "ConsumerMessageCaptureConfig": {
      "type": "object",
      "properties": {
        "enabled": {
          "type": "boolean",
          "default": true
        },
        "keep_last": {
          "type": "integer",
          "format": "uint",
          "minimum": 0,
          "default": 100
        }
      }
    },
    "PublisherClient": {
      "type": "object",
      "properties": {
        "id": {
          "type": "string",
          "default": "01a01af1-8c9f-7f93-9586-dae8b807b3b6"
        },
        "name": {
          "type": "string",
          "default": ""
        },
        "endpoint": {
          "$ref": "#/$defs/Endpoint"
        },
        "comment": {
          "type": "string",
          "default": ""
        },
        "payload": {
          "type": "string"
        },
        "headers": {
          "type": "array",
          "items": {
            "$ref": "#/$defs/HeaderRow"
          }
        },
        "sort_order": {
          "type": [
            "integer",
            "null"
          ],
          "format": "int32"
        }
      },
      "required": [
        "endpoint"
      ]
    },
    "HeaderRow": {
      "type": "object",
      "properties": {
        "key": {
          "type": "string",
          "default": ""
        },
        "value": {
          "type": "string",
          "default": ""
        },
        "enabled": {
          "type": "boolean",
          "default": true
        }
      }
    },
    "ConfigSecurity": {
      "type": "object",
      "properties": {
        "mode": {
          "description": "Configuration security.\nbalanced: Extract secrets to the target secret store and keep message history plain.\nenv_temporary_messages: Extract secrets to env or placeholders and encrypt message history temporarily.\ntemporary_messages: Keep config plain and encrypt message history temporarily.\nsensitive: Encrypt config and encrypt message history temporarily.\ndurable: Encrypt config and keep encrypted message history between restarts when supported.",
          "$ref": "#/$defs/ConfigSecurityMode",
          "default": "balanced"
        }
      }
    },
    "ConfigSecurityMode": {
      "type": "string",
      "enum": [
        "unencrypted",
        "balanced",
        "env_temporary_messages",
        "temporary_messages",
        "sensitive",
        "durable"
      ]
    }
  }
}