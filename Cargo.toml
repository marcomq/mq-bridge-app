[workspace]
members = ["crates/core", "crates/cli"]
default-members = ["crates/cli"]
resolver = "2"

[profile.release]
panic = "abort"

[profile.release-with-lto]
inherits = "release"
lto = true
strip = true
